// SPDX-License-Identifier: Apache-2.0
// <auto-generated />
// Generated by eng/generate_shapes.py; do not edit.
import { toBinary } from "@bufbuild/protobuf";
import { ContentOriginSchema } from "../../gen/arcforges/foundation/v1/foundation_pb.js";
import { NotesQuerySchema } from "../../gen/arcforges/publicapi/v1/content_pb.js";
export function isEntitlementChanged(input: unknown): boolean { return checkEntitlementChanged(input, {active: new Set<object>(), depth: 0}); }
function checkEntitlementChanged(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.entitlementVersion === undefined) return false;
  if (value.entitlementVersion !== undefined) {
    const fieldValue = value.entitlementVersion;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isActorChain(input: unknown): boolean { return checkActorChain(input, {active: new Set<object>(), depth: 0}); }
function checkActorChain(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.initiator === undefined) return false;
  if (value.initiator !== undefined) {
    const fieldValue = value.initiator;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.owner === undefined) return false;
  if (value.owner !== undefined) {
    const fieldValue = value.owner;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.actorKind === undefined) return false;
  if (value.actorKind !== undefined) {
    const fieldValue = value.actorKind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.delegationId !== undefined) {
    const fieldValue = value.delegationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.deviceId !== undefined) {
    const fieldValue = value.deviceId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAggregateRef(input: unknown): boolean { return checkAggregateRef(input, {active: new Set<object>(), depth: 0}); }
function checkAggregateRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length < 1) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.id === undefined) return false;
  if (value.id !== undefined) {
    const fieldValue = value.id;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isApplicationScope(input: unknown): boolean { return checkApplicationScope(input, {active: new Set<object>(), depth: 0}); }
function checkApplicationScope(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.productId === undefined) return false;
  if (value.productId !== undefined) {
    const fieldValue = value.productId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (fieldValue !== "arcnotes" && fieldValue !== "arcscope" && fieldValue !== "arcslate" && fieldValue !== "companion") return false;
  }
  if (value.installationId !== undefined) {
    const fieldValue = value.installationId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isArcError(input: unknown): boolean { return checkArcError(input, {active: new Set<object>(), depth: 0}); }
function checkArcError(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.code === undefined) return false;
  if (value.code !== undefined) {
    const fieldValue = value.code;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.category === undefined) return false;
  if (value.category !== undefined) {
    const fieldValue = value.category;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4 && fieldValue !== 5 && fieldValue !== 6 && fieldValue !== 7 && fieldValue !== 8 && fieldValue !== 9) return false;
  }
  if (value.messageKey === undefined) return false;
  if (value.messageKey !== undefined) {
    const fieldValue = value.messageKey;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.retry === undefined) return false;
  if (value.retry !== undefined) {
    const fieldValue = value.retry;
    if (!checkRetryAdvice(fieldValue, context)) return false;
  }
  if (value.effect === undefined) return false;
  if (value.effect !== undefined) {
    const fieldValue = value.effect;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3) return false;
  }
  if (value.correlationId === undefined) return false;
  if (value.correlationId !== undefined) {
    const fieldValue = value.correlationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.details !== undefined) {
    const fieldValue = value.details;
    if (!checkErrorDetails(fieldValue, context)) return false;
  }
  if (!errorSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isArtifactRef(input: unknown): boolean { return checkArtifactRef(input, {active: new Set<object>(), depth: 0}); }
function checkArtifactRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.artifactId === undefined) return false;
  if (value.artifactId !== undefined) {
    const fieldValue = value.artifactId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.owner === undefined) return false;
  if (value.owner !== undefined) {
    const fieldValue = value.owner;
    if (!checkAggregateRef(fieldValue, context)) return false;
  }
  if (value.resource !== undefined) {
    const fieldValue = value.resource;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if (value.jobId !== undefined) {
    const fieldValue = value.jobId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.producingTaskId !== undefined) {
    const fieldValue = value.producingTaskId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.producingRunId !== undefined) {
    const fieldValue = value.producingRunId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.actor === undefined) return false;
  if (value.actor !== undefined) {
    const fieldValue = value.actor;
    if (!checkActorChain(fieldValue, context)) return false;
  }
  if (value.createdAt === undefined) return false;
  if (value.createdAt !== undefined) {
    const fieldValue = value.createdAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.availability === undefined) return false;
  if (value.availability !== undefined) {
    const fieldValue = value.availability;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.provenance === undefined) return false;
  if (value.provenance !== undefined) {
    const fieldValue = value.provenance;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkVersionedRef(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isBlobRef(input: unknown): boolean { return checkBlobRef(input, {active: new Set<object>(), depth: 0}); }
function checkBlobRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.blobId === undefined) return false;
  if (value.blobId !== undefined) {
    const fieldValue = value.blobId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.contentHash === undefined) return false;
  if (value.contentHash !== undefined) {
    const fieldValue = value.contentHash;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.sizeBytes === undefined) return false;
  if (value.sizeBytes !== undefined) {
    const fieldValue = value.sizeBytes;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isByteRange(input: unknown): boolean { return checkByteRange(input, {active: new Set<object>(), depth: 0}); }
function checkByteRange(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.offset === undefined) return false;
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.length === undefined) return false;
  if (value.length !== undefined) {
    const fieldValue = value.length;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
    if (fieldValue < 1n) return false;
  }
  if ((value.length as bigint) === 0n || (value.offset as bigint) + (value.length as bigint) > 18446744073709551615n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isContentOrigin(input: unknown): boolean { return checkContentOrigin(input, {active: new Set<object>(), depth: 0}); }
function checkContentOrigin(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "arcforges.content-origin.v1") return false;
  }
  if (value.originId === undefined) return false;
  if (value.originId !== undefined) {
    const fieldValue = value.originId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.contentUnitId === undefined) return false;
  if (value.contentUnitId !== undefined) {
    const fieldValue = value.contentUnitId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.kinds === undefined) return false;
  if (value.kinds !== undefined) {
    const fieldValue = value.kinds;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 1) return false;
    if (fieldValue.length > 4) return false;
    if (new Set(fieldValue).size !== fieldValue.length) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    if (item !== "aiGenerated" && item !== "aiManipulated" && item !== "nonAi" && item !== "unknown") return false;
    }
  }
  if (value.payloadSha256 === undefined) return false;
  if (value.payloadSha256 !== undefined) {
    const fieldValue = value.payloadSha256;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.producerKind === undefined) return false;
  if (value.producerKind !== undefined) {
    const fieldValue = value.producerKind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "model" && fieldValue !== "human" && fieldValue !== "deterministic" && fieldValue !== "import") return false;
  }
  if (value.createdAt !== undefined) {
    const fieldValue = value.createdAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.parentOriginIds === undefined) return false;
  if (value.parentOriginIds !== undefined) {
    const fieldValue = value.parentOriginIds;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 32) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.omittedParentCount === undefined) return false;
  if (value.omittedParentCount !== undefined) {
    const fieldValue = value.omittedParentCount;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue > 2147483647) return false;
  }
  if (!originSemantics(value) || toBinary(ContentOriginSchema, value as never).length > 65536) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isDecimal(input: unknown): boolean { return checkDecimal(input, {active: new Set<object>(), depth: 0}); }
function checkDecimal(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (!canonicalDecimal(value.value as string)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isErrorDetails(input: unknown): boolean { return checkErrorDetails(input, {active: new Set<object>(), depth: 0}); }
function checkErrorDetails(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.detail as {case?: string} | undefined)?.case === "revision") {
    const fieldValue = (value.detail as {value: unknown}).value;
    if (!checkRevisionConflict(fieldValue, context)) return false;
  }
  if ((value.detail as {case?: string} | undefined)?.case === "limit") {
    const fieldValue = (value.detail as {value: unknown}).value;
    if (!checkLimitFailure(fieldValue, context)) return false;
  }
  if ((value.detail as {case?: string} | undefined)?.case === "upgrade") {
    const fieldValue = (value.detail as {value: unknown}).value;
    if (!checkVersionFailure(fieldValue, context)) return false;
  }
  if ((value.detail as {case?: string} | undefined)?.case === "state") {
    const fieldValue = (value.detail as {value: unknown}).value;
    if (!checkStateFailure(fieldValue, context)) return false;
  }
  if (value.detail === undefined || typeof value.detail !== 'object' || value.detail === null) return false;
  if ((value.detail as {case?: string}).case !== "revision" && (value.detail as {case?: string}).case !== "limit" && (value.detail as {case?: string}).case !== "upgrade" && (value.detail as {case?: string}).case !== "state") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isId(input: unknown): boolean { return checkId(input, {active: new Set<object>(), depth: 0}); }
function checkId(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!(fieldValue instanceof Uint8Array)) return false;
    if (fieldValue.length !== 16) return false;
  }
  if (!(value.value as Uint8Array).some(v => v !== 0)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isInstant(input: unknown): boolean { return checkInstant(input, {active: new Set<object>(), depth: 0}); }
function checkInstant(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.unixSeconds === undefined) return false;
  if (value.unixSeconds !== undefined) {
    const fieldValue = value.unixSeconds;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
    if (fieldValue < -62135596800n) return false;
    if (fieldValue > 253402300799n) return false;
  }
  if (value.nanos === undefined) return false;
  if (value.nanos !== undefined) {
    const fieldValue = value.nanos;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 0) return false;
    if (fieldValue > 999999999) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isLimitFailure(input: unknown): boolean { return checkLimitFailure(input, {active: new Set<object>(), depth: 0}); }
function checkLimitFailure(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.limitName === undefined) return false;
  if (value.limitName !== undefined) {
    const fieldValue = value.limitName;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.maximum !== undefined) {
    const fieldValue = value.maximum;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if (value.available !== undefined) {
    const fieldValue = value.available;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if (value.recoveryAt !== undefined) {
    const fieldValue = value.recoveryAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isLocalNotesVersion(input: unknown): boolean { return checkLocalNotesVersion(input, {active: new Set<object>(), depth: 0}); }
function checkLocalNotesVersion(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.ackedRev === undefined) return false;
  if (value.ackedRev !== undefined) {
    const fieldValue = value.ackedRev;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.headLocalSeq === undefined) return false;
  if (value.headLocalSeq !== undefined) {
    const fieldValue = value.headLocalSeq;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaRange(input: unknown): boolean { return checkMediaRange(input, {active: new Set<object>(), depth: 0}); }
function checkMediaRange(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.start === undefined) return false;
  if (value.start !== undefined) {
    const fieldValue = value.start;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.duration === undefined) return false;
  if (value.duration !== undefined) {
    const fieldValue = value.duration;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (!mediaRangeSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaTime(input: unknown): boolean { return checkMediaTime(input, {active: new Set<object>(), depth: 0}); }
function checkMediaTime(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.ticks === undefined) return false;
  if (value.ticks !== undefined) {
    const fieldValue = value.ticks;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if (value.rate === undefined) return false;
  if (value.rate !== undefined) {
    const fieldValue = value.rate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if ((value.rate as Profile).numerator !== 705600000n || (value.rate as Profile).denominator !== 1n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNativeContentRev(input: unknown): boolean { return checkNativeContentRev(input, {active: new Set<object>(), depth: 0}); }
function checkNativeContentRev(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPageRequest(input: unknown): boolean { return checkPageRequest(input, {active: new Set<object>(), depth: 0}); }
function checkPageRequest(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.cursor !== undefined) {
    const fieldValue = value.cursor;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 4096) return false;
  }
  if (value.limit !== undefined) {
    const fieldValue = value.limit;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue < 1) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPageState(input: unknown): boolean { return checkPageState(input, {active: new Set<object>(), depth: 0}); }
function checkPageState(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.nextCursor !== undefined) {
    const fieldValue = value.nextCursor;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 4096) return false;
  }
  if (value.hasMore === undefined) return false;
  if (value.hasMore !== undefined) {
    const fieldValue = value.hasMore;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.hasMore === true && (typeof value.nextCursor !== 'string' || value.nextCursor.length === 0)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRational(input: unknown): boolean { return checkRational(input, {active: new Set<object>(), depth: 0}); }
function checkRational(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.numerator === undefined) return false;
  if (value.numerator !== undefined) {
    const fieldValue = value.numerator;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if (value.denominator === undefined) return false;
  if (value.denominator !== undefined) {
    const fieldValue = value.denominator;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
    if (fieldValue < 1n) return false;
  }
  if (!reduced(value.numerator as bigint, value.denominator as bigint)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isReceipt(input: unknown): boolean { return checkReceipt(input, {active: new Set<object>(), depth: 0}); }
function checkReceipt(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.commandId === undefined) return false;
  if (value.commandId !== undefined) {
    const fieldValue = value.commandId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.resultRevision !== undefined) {
    const fieldValue = value.resultRevision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.effect === undefined) return false;
  if (value.effect !== undefined) {
    const fieldValue = value.effect;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRequestMeta(input: unknown): boolean { return checkRequestMeta(input, {active: new Set<object>(), depth: 0}); }
function checkRequestMeta(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.commandId !== undefined) {
    const fieldValue = value.commandId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.expectedRev !== undefined) {
    const fieldValue = value.expectedRev;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.correlationId === undefined) return false;
  if (value.correlationId !== undefined) {
    const fieldValue = value.correlationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.workspaceId !== undefined) {
    const fieldValue = value.workspaceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.expectedLocal !== undefined) {
    const fieldValue = value.expectedLocal;
    if (!checkLocalNotesVersion(fieldValue, context)) return false;
  }
  if (value.expectedNative !== undefined) {
    const fieldValue = value.expectedNative;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if (value.recoveryGeneration !== undefined) {
    const fieldValue = value.recoveryGeneration;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.applicationScope !== undefined) {
    const fieldValue = value.applicationScope;
    if (!checkApplicationScope(fieldValue, context)) return false;
  }
  if ([value.expectedRev, value.expectedLocal, value.expectedNative].filter(v => v !== undefined).length > 1) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isResourceRef(input: unknown): boolean { return checkResourceRef(input, {active: new Set<object>(), depth: 0}); }
function checkResourceRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.realmId === undefined) return false;
  if (value.realmId !== undefined) {
    const fieldValue = value.realmId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.workspaceId !== undefined) {
    const fieldValue = value.workspaceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.ownerAppId === undefined) return false;
  if (value.ownerAppId !== undefined) {
    const fieldValue = value.ownerAppId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.resourceKind === undefined) return false;
  if (value.resourceKind !== undefined) {
    const fieldValue = value.resourceKind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.resourceId === undefined) return false;
  if (value.resourceId !== undefined) {
    const fieldValue = value.resourceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.displayHint !== undefined) {
    const fieldValue = value.displayHint;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.availability === undefined) return false;
  if (value.availability !== undefined) {
    const fieldValue = value.availability;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4 && fieldValue !== 5) return false;
  }
  if (value.holdingDeviceId !== undefined) {
    const fieldValue = value.holdingDeviceId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isResourceVersionRef(input: unknown): boolean { return checkResourceVersionRef(input, {active: new Set<object>(), depth: 0}); }
function checkResourceVersionRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.resource === undefined) return false;
  if (value.resource !== undefined) {
    const fieldValue = value.resource;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if ((value.revision as {case?: string} | undefined)?.case === "cloud") {
    const fieldValue = (value.revision as {value: unknown}).value;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if ((value.revision as {case?: string} | undefined)?.case === "native") {
    const fieldValue = (value.revision as {value: unknown}).value;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if ((value.revision as {case?: string} | undefined)?.case === "local") {
    const fieldValue = (value.revision as {value: unknown}).value;
    if (!checkLocalNotesVersion(fieldValue, context)) return false;
  }
  if (value.contentHash !== undefined) {
    const fieldValue = value.contentHash;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.blob !== undefined) {
    const fieldValue = value.blob;
    if (!checkBlobRef(fieldValue, context)) return false;
  }
  if (value.revision === undefined || typeof value.revision !== 'object' || value.revision === null) return false;
  if ((value.revision as {case?: string}).case !== "cloud" && (value.revision as {case?: string}).case !== "native" && (value.revision as {case?: string}).case !== "local") return false;
  if (!resourceVersionSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isResponseMeta(input: unknown): boolean { return checkResponseMeta(input, {active: new Set<object>(), depth: 0}); }
function checkResponseMeta(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.resultRev !== undefined) {
    const fieldValue = value.resultRev;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.entitlementVersion !== undefined) {
    const fieldValue = value.entitlementVersion;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if (value.warnings === undefined) return false;
  if (value.warnings !== undefined) {
    const fieldValue = value.warnings;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    }
  }
  if (value.correlationId === undefined) return false;
  if (value.correlationId !== undefined) {
    const fieldValue = value.correlationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.recoveryGeneration !== undefined) {
    const fieldValue = value.recoveryGeneration;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRetryAdvice(input: unknown): boolean { return checkRetryAdvice(input, {active: new Set<object>(), depth: 0}); }
function checkRetryAdvice(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.mode === undefined) return false;
  if (value.mode !== undefined) {
    const fieldValue = value.mode;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4) return false;
  }
  if (value.retryAt !== undefined) {
    const fieldValue = value.retryAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.reconciliationOperation !== undefined) {
    const fieldValue = value.reconciliationOperation;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if ((value.mode === 3) !== (value.retryAt !== undefined) || (value.mode === 4) !== (value.reconciliationOperation !== undefined)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRevision(input: unknown): boolean { return checkRevision(input, {active: new Set<object>(), depth: 0}); }
function checkRevision(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
    if (fieldValue < 0n) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRevisionConflict(input: unknown): boolean { return checkRevisionConflict(input, {active: new Set<object>(), depth: 0}); }
function checkRevisionConflict(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.expected !== undefined) {
    const fieldValue = value.expected;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.actual !== undefined) {
    const fieldValue = value.actual;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.conflictId !== undefined) {
    const fieldValue = value.conflictId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isStateFailure(input: unknown): boolean { return checkStateFailure(input, {active: new Set<object>(), depth: 0}); }
function checkStateFailure(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.state === undefined) return false;
  if (value.state !== undefined) {
    const fieldValue = value.state;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.reason === undefined) return false;
  if (value.reason !== undefined) {
    const fieldValue = value.reason;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.resource !== undefined) {
    const fieldValue = value.resource;
    if (!checkAggregateRef(fieldValue, context)) return false;
  }
  if (value.local !== undefined) {
    const fieldValue = value.local;
    if (!checkLocalNotesVersion(fieldValue, context)) return false;
  }
  if (value.native !== undefined) {
    const fieldValue = value.native;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTimeRangeUtc(input: unknown): boolean { return checkTimeRangeUtc(input, {active: new Set<object>(), depth: 0}); }
function checkTimeRangeUtc(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.from === undefined) return false;
  if (value.from !== undefined) {
    const fieldValue = value.from;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.until === undefined) return false;
  if (value.until !== undefined) {
    const fieldValue = value.until;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (compareInstant(value.from as Profile, value.until as Profile) > 0) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isVersionFailure(input: unknown): boolean { return checkVersionFailure(input, {active: new Set<object>(), depth: 0}); }
function checkVersionFailure(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.minReadable === undefined) return false;
  if (value.minReadable !== undefined) {
    const fieldValue = value.minReadable;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.minWritable === undefined) return false;
  if (value.minWritable !== undefined) {
    const fieldValue = value.minWritable;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.received === undefined) return false;
  if (value.received !== undefined) {
    const fieldValue = value.received;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isVersionedRef(input: unknown): boolean { return checkVersionedRef(input, {active: new Set<object>(), depth: 0}); }
function checkVersionedRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.root === undefined) return false;
  if (value.root !== undefined) {
    const fieldValue = value.root;
    if (!checkAggregateRef(fieldValue, context)) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.sha256 !== undefined) {
    const fieldValue = value.sha256;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if ((value.revision as {value: bigint}).value <= 0n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAgentProfile(input: unknown): boolean { return checkAgentProfile(input, {active: new Set<object>(), depth: 0}); }
function checkAgentProfile(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.profileId === undefined) return false;
  if (value.profileId !== undefined) {
    const fieldValue = value.profileId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.modelId === undefined) return false;
  if (value.modelId !== undefined) {
    const fieldValue = value.modelId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
    if ((new RegExp("^([A-Za-z0-9._:/-]{1,128}|@cf/[a-z0-9._-]+/[a-zA-Z0-9._/-]+)$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.instructions === undefined) return false;
  if (value.instructions !== undefined) {
    const fieldValue = value.instructions;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.skillIds === undefined) return false;
  if (value.skillIds !== undefined) {
    const fieldValue = value.skillIds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.context === undefined) return false;
  if (value.context !== undefined) {
    const fieldValue = value.context;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  if (value.capabilities === undefined) return false;
  if (value.capabilities !== undefined) {
    const fieldValue = value.capabilities;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAggregateBody(input: unknown): boolean { return checkAggregateBody(input, {active: new Set<object>(), depth: 0}); }
function checkAggregateBody(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.body as {case?: string} | undefined)?.case === "notes") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkNotesDocument(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "chat") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkConversationBody(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "scopeMetadata") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkScopeMetadata(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "slateMetadata") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkSlateMetadata(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "notebook") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkNotebookBody(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "propertyDefinition") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkPropertyDefinition(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "savedView") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkSavedViewRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "tag") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkTagRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "agentProfile") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkAgentProfile(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "skill") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkSkillRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "chatProject") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkChatProjectRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "preferences") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkPreferenceRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "task") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkTaskSnapshot(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "automation") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkAutomationView(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "memory") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkMemoryRecord(fieldValue, context)) return false;
  }
  if ((value.body as {case?: string} | undefined)?.case === "externalBody") {
    const fieldValue = (value.body as {value: unknown}).value;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.body === undefined || typeof value.body !== 'object' || value.body === null) return false;
  if ((value.body as {case?: string}).case !== "notes" && (value.body as {case?: string}).case !== "chat" && (value.body as {case?: string}).case !== "scopeMetadata" && (value.body as {case?: string}).case !== "slateMetadata" && (value.body as {case?: string}).case !== "notebook" && (value.body as {case?: string}).case !== "propertyDefinition" && (value.body as {case?: string}).case !== "savedView" && (value.body as {case?: string}).case !== "tag" && (value.body as {case?: string}).case !== "agentProfile" && (value.body as {case?: string}).case !== "skill" && (value.body as {case?: string}).case !== "chatProject" && (value.body as {case?: string}).case !== "preferences" && (value.body as {case?: string}).case !== "task" && (value.body as {case?: string}).case !== "automation" && (value.body as {case?: string}).case !== "memory" && (value.body as {case?: string}).case !== "externalBody") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAlignmentSpec(input: unknown): boolean { return checkAlignmentSpec(input, {active: new Set<object>(), depth: 0}); }
function checkAlignmentSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "absoluteTime" && fieldValue !== "trigger" && fieldValue !== "event" && fieldValue !== "manualOffset") return false;
  }
  if (value.leftAnchor !== undefined) {
    const fieldValue = value.leftAnchor;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.rightAnchor !== undefined) {
    const fieldValue = value.rightAnchor;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.eventId !== undefined) {
    const fieldValue = value.eventId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (!alignmentSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAutomationSpec(input: unknown): boolean { return checkAutomationSpec(input, {active: new Set<object>(), depth: 0}); }
function checkAutomationSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.trigger === undefined) return false;
  if (value.trigger !== undefined) {
    const fieldValue = value.trigger;
    if (!checkTriggerSpec(fieldValue, context)) return false;
  }
  if (value.input === undefined) return false;
  if (value.input !== undefined) {
    const fieldValue = value.input;
    if (!checkTurnInput(fieldValue, context)) return false;
  }
  if (value.profileId === undefined) return false;
  if (value.profileId !== undefined) {
    const fieldValue = value.profileId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.perRunBudget === undefined) return false;
  if (value.perRunBudget !== undefined) {
    const fieldValue = value.perRunBudget;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.periodBudget === undefined) return false;
  if (value.periodBudget !== undefined) {
    const fieldValue = value.periodBudget;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.budgetPeriod === undefined) return false;
  if (value.budgetPeriod !== undefined) {
    const fieldValue = value.budgetPeriod;
    if (!checkTimeRangeUtc(fieldValue, context)) return false;
  }
  if (value.concurrency === undefined) return false;
  if (value.concurrency !== undefined) {
    const fieldValue = value.concurrency;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.misfire === undefined) return false;
  if (value.misfire !== undefined) {
    const fieldValue = value.misfire;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.catchUpCount === undefined) return false;
  if (value.catchUpCount !== undefined) {
    const fieldValue = value.catchUpCount;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.catchUpWindowSeconds === undefined) return false;
  if (value.catchUpWindowSeconds !== undefined) {
    const fieldValue = value.catchUpWindowSeconds;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.grants === undefined) return false;
  if (value.grants !== undefined) {
    const fieldValue = value.grants;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.targetDeviceId !== undefined) {
    const fieldValue = value.targetDeviceId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isAutomationView(input: unknown): boolean { return checkAutomationView(input, {active: new Set<object>(), depth: 0}); }
function checkAutomationView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.automationId === undefined) return false;
  if (value.automationId !== undefined) {
    const fieldValue = value.automationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.definition === undefined) return false;
  if (value.definition !== undefined) {
    const fieldValue = value.definition;
    if (!checkAutomationSpec(fieldValue, context)) return false;
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.nextOccurrence !== undefined) {
    const fieldValue = value.nextOccurrence;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isBlock(input: unknown): boolean { return checkBlock(input, {active: new Set<object>(), depth: 0}); }
function checkBlock(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.blockId === undefined) return false;
  if (value.blockId !== undefined) {
    const fieldValue = value.blockId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.parentId !== undefined) {
    const fieldValue = value.parentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.orderKey === undefined) return false;
  if (value.orderKey !== undefined) {
    const fieldValue = value.orderKey;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "paragraph" && fieldValue !== "heading" && fieldValue !== "list" && fieldValue !== "quote" && fieldValue !== "callout" && fieldValue !== "toggle" && fieldValue !== "code" && fieldValue !== "math" && fieldValue !== "divider" && fieldValue !== "table" && fieldValue !== "image" && fieldValue !== "attachment" && fieldValue !== "embed") return false;
  }
  if (value.body === undefined) return false;
  if (value.body !== undefined) {
    const fieldValue = value.body;
    if (!checkBlockBody(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.properties !== undefined) {
    const fieldValue = value.properties;
    if (!checkBlockProperties(fieldValue, context)) return false;
  }
  if (!blockSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isBlockBody(input: unknown): boolean { return checkBlockBody(input, {active: new Set<object>(), depth: 0}); }
function checkBlockBody(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.content as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkRichText(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "code") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkCodeBlock(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "resource") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "link") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkLinkSpec(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "table") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkTableBlock(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "math") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkMathContent(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "empty") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.content === undefined || typeof value.content !== 'object' || value.content === null) return false;
  if ((value.content as {case?: string}).case !== "text" && (value.content as {case?: string}).case !== "code" && (value.content as {case?: string}).case !== "resource" && (value.content as {case?: string}).case !== "link" && (value.content as {case?: string}).case !== "table" && (value.content as {case?: string}).case !== "math" && (value.content as {case?: string}).case !== "empty") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isBlockProperties(input: unknown): boolean { return checkBlockProperties(input, {active: new Set<object>(), depth: 0}); }
function checkBlockProperties(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.headingLevel !== undefined) {
    const fieldValue = value.headingLevel;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
    if (fieldValue > 6) return false;
  }
  if (value.checked !== undefined) {
    const fieldValue = value.checked;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.calloutKind !== undefined) {
    const fieldValue = value.calloutKind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "info" && fieldValue !== "warning" && fieldValue !== "error" && fieldValue !== "success") return false;
  }
  if (value.listStyle !== undefined) {
    const fieldValue = value.listStyle;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "bulleted" && fieldValue !== "numbered" && fieldValue !== "checklist") return false;
  }
  if (value.altText !== undefined) {
    const fieldValue = value.altText;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.imageLayout !== undefined) {
    const fieldValue = value.imageLayout;
    if (!checkImageLayout(fieldValue, context)) return false;
  }
  if (value.attachmentPresentation !== undefined) {
    const fieldValue = value.attachmentPresentation;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "chip" && fieldValue !== "card" && fieldValue !== "pdfViewer") return false;
  }
  if (value.embedRenderMode !== undefined) {
    const fieldValue = value.embedRenderMode;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "inline" && fieldValue !== "card") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCalibration(input: unknown): boolean { return checkCalibration(input, {active: new Set<object>(), depth: 0}); }
function checkCalibration(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.scale === undefined) return false;
  if (value.scale !== undefined) {
    const fieldValue = value.scale;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.offset === undefined) return false;
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.unit === undefined) return false;
  if (value.unit !== undefined) {
    const fieldValue = value.unit;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCapabilityArguments(input: unknown): boolean { return checkCapabilityArguments(input, {active: new Set<object>(), depth: 0}); }
function checkCapabilityArguments(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.schemaId === undefined) return false;
  if (value.schemaId !== undefined) {
    const fieldValue = value.schemaId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkStructuredValue(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCapabilityResult(input: unknown): boolean { return checkCapabilityResult(input, {active: new Set<object>(), depth: 0}); }
function checkCapabilityResult(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.schemaId === undefined) return false;
  if (value.schemaId !== undefined) {
    const fieldValue = value.schemaId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkStructuredValue(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCaptureMetadata(input: unknown): boolean { return checkCaptureMetadata(input, {active: new Set<object>(), depth: 0}); }
function checkCaptureMetadata(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.captureId === undefined) return false;
  if (value.captureId !== undefined) {
    const fieldValue = value.captureId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.sampleCount === undefined) return false;
  if (value.sampleCount !== undefined) {
    const fieldValue = value.sampleCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.channels === undefined) return false;
  if (value.channels !== undefined) {
    const fieldValue = value.channels;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkChannelDefinition(item, context)) return false;
    }
  }
  if (value.contentHash !== undefined) {
    const fieldValue = value.contentHash;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.resource !== undefined) {
    const fieldValue = value.resource;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isChannelDefinition(input: unknown): boolean { return checkChannelDefinition(input, {active: new Set<object>(), depth: 0}); }
function checkChannelDefinition(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.unit === undefined) return false;
  if (value.unit !== undefined) {
    const fieldValue = value.unit;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.sampleType === undefined) return false;
  if (value.sampleType !== undefined) {
    const fieldValue = value.sampleType;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.rate === undefined) return false;
  if (value.rate !== undefined) {
    const fieldValue = value.rate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.calibration !== undefined) {
    const fieldValue = value.calibration;
    if (!checkCalibration(fieldValue, context)) return false;
  }
  if ((value.rate as Profile).numerator <= 0n || (value.calibration !== undefined && (value.calibration as Profile).unit !== value.unit)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isChatProjectRecord(input: unknown): boolean { return checkChatProjectRecord(input, {active: new Set<object>(), depth: 0}); }
function checkChatProjectRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.projectId === undefined) return false;
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.conversationIds === undefined) return false;
  if (value.conversationIds !== undefined) {
    const fieldValue = value.conversationIds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.context === undefined) return false;
  if (value.context !== undefined) {
    const fieldValue = value.context;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isChecksumSpec(input: unknown): boolean { return checkChecksumSpec(input, {active: new Set<object>(), depth: 0}); }
function checkChecksumSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.algorithm === undefined) return false;
  if (value.algorithm !== undefined) {
    const fieldValue = value.algorithm;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "xor8" && fieldValue !== "crc16CcittFalse" && fieldValue !== "crc32IsoHdlc") return false;
  }
  if (value.input === undefined) return false;
  if (value.input !== undefined) {
    const fieldValue = value.input;
    if (!checkByteRange(fieldValue, context)) return false;
  }
  if (value.offset === undefined) return false;
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.byteOrder === undefined) return false;
  if (value.byteOrder !== undefined) {
    const fieldValue = value.byteOrder;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "little" && fieldValue !== "big") return false;
  }
  if (!checksumSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCodeBlock(input: unknown): boolean { return checkCodeBlock(input, {active: new Set<object>(), depth: 0}); }
function checkCodeBlock(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.language !== undefined) {
    const fieldValue = value.language;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.wrap === undefined) return false;
  if (value.wrap !== undefined) {
    const fieldValue = value.wrap;
    if (typeof fieldValue !== "boolean") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isColourConfiguration(input: unknown): boolean { return checkColourConfiguration(input, {active: new Set<object>(), depth: 0}); }
function checkColourConfiguration(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.workingSpace === undefined) return false;
  if (value.workingSpace !== undefined) {
    const fieldValue = value.workingSpace;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.displaySpace === undefined) return false;
  if (value.displaySpace !== undefined) {
    const fieldValue = value.displaySpace;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.ocioConfig !== undefined) {
    const fieldValue = value.ocioConfig;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.inputAssignments === undefined) return false;
  if (value.inputAssignments !== undefined) {
    const fieldValue = value.inputAssignments;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMediaColourAssignment(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isContextRef(input: unknown): boolean { return checkContextRef(input, {active: new Set<object>(), depth: 0}); }
function checkContextRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.source === undefined) return false;
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkAggregateRef(fieldValue, context)) return false;
  }
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.resource !== undefined) {
    const fieldValue = value.resource;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if (value.selector !== undefined) {
    const fieldValue = value.selector;
    if (!checkContextSelector(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.lifetime === undefined) return false;
  if (value.lifetime !== undefined) {
    const fieldValue = value.lifetime;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4) return false;
  }
  if (value.ephemeral !== undefined) {
    const fieldValue = value.ephemeral;
    if (!checkEphemeralSelection(fieldValue, context)) return false;
  }
  if (value.consent !== undefined) {
    const fieldValue = value.consent;
    if (!checkSourceConsentRef(fieldValue, context)) return false;
  }
  if (value.revision !== undefined && (value.revision as Profile).value <= 0n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isContextSelector(input: unknown): boolean { return checkContextSelector(input, {active: new Set<object>(), depth: 0}); }
function checkContextSelector(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.selection as {case?: string} | undefined)?.case === "whole") {
    const fieldValue = (value.selection as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.selection as {case?: string} | undefined)?.case === "byteRange") {
    const fieldValue = (value.selection as {value: unknown}).value;
    if (!checkByteRange(fieldValue, context)) return false;
  }
  if ((value.selection as {case?: string} | undefined)?.case === "notes") {
    const fieldValue = (value.selection as {value: unknown}).value;
    if (!checkNotesSelection(fieldValue, context)) return false;
  }
  if ((value.selection as {case?: string} | undefined)?.case === "scope") {
    const fieldValue = (value.selection as {value: unknown}).value;
    if (!checkScopeSelection(fieldValue, context)) return false;
  }
  if ((value.selection as {case?: string} | undefined)?.case === "slate") {
    const fieldValue = (value.selection as {value: unknown}).value;
    if (!checkSlateSelection(fieldValue, context)) return false;
  }
  if (value.selection === undefined || typeof value.selection !== 'object' || value.selection === null) return false;
  if ((value.selection as {case?: string}).case !== "whole" && (value.selection as {case?: string}).case !== "byteRange" && (value.selection as {case?: string}).case !== "notes" && (value.selection as {case?: string}).case !== "scope" && (value.selection as {case?: string}).case !== "slate") return false;
  if ((value.selection as Profile).case === 'whole' && (value.selection as Profile).value !== true) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isControlProgress(input: unknown): boolean { return checkControlProgress(input, {active: new Set<object>(), depth: 0}); }
function checkControlProgress(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.commandId === undefined) return false;
  if (value.commandId !== undefined) {
    const fieldValue = value.commandId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3) return false;
  }
  if (value.state === undefined) return false;
  if (value.state !== undefined) {
    const fieldValue = value.state;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4) return false;
  }
  if (value.requestedAt === undefined) return false;
  if (value.requestedAt !== undefined) {
    const fieldValue = value.requestedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.acknowledgedAt !== undefined) {
    const fieldValue = value.acknowledgedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.reason !== undefined) {
    const fieldValue = value.reason;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isConversationBody(input: unknown): boolean { return checkConversationBody(input, {active: new Set<object>(), depth: 0}); }
function checkConversationBody(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.conversation === undefined) return false;
  if (value.conversation !== undefined) {
    const fieldValue = value.conversation;
    if (!checkConversationView(fieldValue, context)) return false;
  }
  if (value.messages === undefined) return false;
  if (value.messages !== undefined) {
    const fieldValue = value.messages;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMessageView(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isConversationView(input: unknown): boolean { return checkConversationView(input, {active: new Set<object>(), depth: 0}); }
function checkConversationView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.conversationId === undefined) return false;
  if (value.conversationId !== undefined) {
    const fieldValue = value.conversationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.branchId === undefined) return false;
  if (value.branchId !== undefined) {
    const fieldValue = value.branchId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.title === undefined) return false;
  if (value.title !== undefined) {
    const fieldValue = value.title;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.lastMessageAt !== undefined) {
    const fieldValue = value.lastMessageAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.pinned === undefined) return false;
  if (value.pinned !== undefined) {
    const fieldValue = value.pinned;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.archived === undefined) return false;
  if (value.archived !== undefined) {
    const fieldValue = value.archived;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.temporary === undefined) return false;
  if (value.temporary !== undefined) {
    const fieldValue = value.temporary;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.applicationScope === undefined) return false;
  if (value.applicationScope !== undefined) {
    const fieldValue = value.applicationScope;
    if (!checkApplicationScope(fieldValue, context)) return false;
  }
  if (value.historyMode === undefined) return false;
  if (value.historyMode !== undefined) {
    const fieldValue = value.historyMode;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCursorResult(input: unknown): boolean { return checkCursorResult(input, {active: new Set<object>(), depth: 0}); }
function checkCursorResult(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.a === undefined) return false;
  if (value.a !== undefined) {
    const fieldValue = value.a;
    if (!checkSelectedSample(fieldValue, context)) return false;
  }
  if (value.b === undefined) return false;
  if (value.b !== undefined) {
    const fieldValue = value.b;
    if (!checkSelectedSample(fieldValue, context)) return false;
  }
  if (value.deltaTime === undefined) return false;
  if (value.deltaTime !== undefined) {
    const fieldValue = value.deltaTime;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.deltaValue === undefined) return false;
  if (value.deltaValue !== undefined) {
    const fieldValue = value.deltaValue;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (!cursorSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isCursorSpec(input: unknown): boolean { return checkCursorSpec(input, {active: new Set<object>(), depth: 0}); }
function checkCursorSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.segmentId === undefined) return false;
  if (value.segmentId !== undefined) {
    const fieldValue = value.segmentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.time === undefined) return false;
  if (value.time !== undefined) {
    const fieldValue = value.time;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isEffectParameter(input: unknown): boolean { return checkEffectParameter(input, {active: new Set<object>(), depth: 0}); }
function checkEffectParameter(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "decimal") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "boolean") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "keyframes") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkKeyframeList(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "rational") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.value === undefined || typeof value.value !== 'object' || value.value === null) return false;
  if ((value.value as {case?: string}).case !== "decimal" && (value.value as {case?: string}).case !== "boolean" && (value.value as {case?: string}).case !== "text" && (value.value as {case?: string}).case !== "keyframes" && (value.value as {case?: string}).case !== "rational") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isEffectSpec(input: unknown): boolean { return checkEffectSpec(input, {active: new Set<object>(), depth: 0}); }
function checkEffectSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.parameters === undefined) return false;
  if (value.parameters !== undefined) {
    const fieldValue = value.parameters;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkEffectParameter(item, context)) return false;
    }
  }
  if (value.instanceId === undefined) return false;
  if (value.instanceId !== undefined) {
    const fieldValue = value.instanceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.definitionVersion === undefined) return false;
  if (value.definitionVersion !== undefined) {
    const fieldValue = value.definitionVersion;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.scope === undefined) return false;
  if (value.scope !== undefined) {
    const fieldValue = value.scope;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "clipLocal" && fieldValue !== "sequence") return false;
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.retainedDefinition !== undefined) {
    const fieldValue = value.retainedDefinition;
    if (!(fieldValue instanceof Uint8Array)) return false;
    if (fieldValue.length > 65536) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isEphemeralSelection(input: unknown): boolean { return checkEphemeralSelection(input, {active: new Set<object>(), depth: 0}); }
function checkEphemeralSelection(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.instanceId === undefined) return false;
  if (value.instanceId !== undefined) {
    const fieldValue = value.instanceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.generation === undefined) return false;
  if (value.generation !== undefined) {
    const fieldValue = value.generation;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.selectionId === undefined) return false;
  if (value.selectionId !== undefined) {
    const fieldValue = value.selectionId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.expiresAt === undefined) return false;
  if (value.expiresAt !== undefined) {
    const fieldValue = value.expiresAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isEventTrigger(input: unknown): boolean { return checkEventTrigger(input, {active: new Set<object>(), depth: 0}); }
function checkEventTrigger(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkAggregateRef(fieldValue, context)) return false;
  }
  if (value.predicate !== undefined) {
    const fieldValue = value.predicate;
    if (!checkNotesFilter(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isFamilyResult(input: unknown): boolean { return checkFamilyResult(input, {active: new Set<object>(), depth: 0}); }
function checkFamilyResult(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.family === undefined) return false;
  if (value.family !== undefined) {
    const fieldValue = value.family;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "minimum" && fieldValue !== "maximum" && fieldValue !== "mean" && fieldValue !== "rms" && fieldValue !== "peakToPeak" && fieldValue !== "standardDeviation" && fieldValue !== "count" && fieldValue !== "duration" && fieldValue !== "frequency" && fieldValue !== "dutyCycle" && fieldValue !== "riseTime" && fieldValue !== "fallTime" && fieldValue !== "cursorDelta" && fieldValue !== "eventCount") return false;
  }
  if (value.status === undefined) return false;
  if (value.status !== undefined) {
    const fieldValue = value.status;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "ok" && fieldValue !== "insufficient" && fieldValue !== "invalid") return false;
  }
  if (value.values === undefined) return false;
  if (value.values !== undefined) {
    const fieldValue = value.values;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 2) return false;
    for (const item of fieldValue) {
    if (!checkMeasurementValue(item, context)) return false;
    }
  }
  if (value.validCount === undefined) return false;
  if (value.validCount !== undefined) {
    const fieldValue = value.validCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.invalidCount === undefined) return false;
  if (value.invalidCount !== undefined) {
    const fieldValue = value.invalidCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.reason !== undefined) {
    const fieldValue = value.reason;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (fieldValue !== "noFiniteSamples" && fieldValue !== "noCompleteCycle" && fieldValue !== "noCompleteEdge" && fieldValue !== "cursorUnavailable" && fieldValue !== "invalidTimeOrder" && fieldValue !== "invalidConfiguration" && fieldValue !== "numericOverflow") return false;
  }
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (!familySemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isFilterGroup(input: unknown): boolean { return checkFilterGroup(input, {active: new Set<object>(), depth: 0}); }
function checkFilterGroup(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.operator === undefined) return false;
  if (value.operator !== undefined) {
    const fieldValue = value.operator;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "all" && fieldValue !== "any" && fieldValue !== "not") return false;
  }
  if (value.children === undefined) return false;
  if (value.children !== undefined) {
    const fieldValue = value.children;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 1) return false;
    if (fieldValue.length > 32) return false;
    for (const item of fieldValue) {
    if (!checkNotesFilter(item, context)) return false;
    }
  }
  if (value.operator === 'not' ? (value.children as unknown[]).length !== 1 : !['all','any'].includes(value.operator as string) || (value.children as unknown[]).length < 1 || (value.children as unknown[]).length > 32) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isFolderView(input: unknown): boolean { return checkFolderView(input, {active: new Set<object>(), depth: 0}); }
function checkFolderView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.folderId === undefined) return false;
  if (value.folderId !== undefined) {
    const fieldValue = value.folderId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.parentId !== undefined) {
    const fieldValue = value.parentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.orderKey === undefined) return false;
  if (value.orderKey !== undefined) {
    const fieldValue = value.orderKey;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.notebookRev === undefined) return false;
  if (value.notebookRev !== undefined) {
    const fieldValue = value.notebookRev;
    if (!checkRevision(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isFrameConfiguration(input: unknown): boolean { return checkFrameConfiguration(input, {active: new Set<object>(), depth: 0}); }
function checkFrameConfiguration(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "delimitedText" && fieldValue !== "csvLine" && fieldValue !== "jsonLine" && fieldValue !== "fixedBinary" && fieldValue !== "canonicalReplay") return false;
  }
  if (value.start === undefined) return false;
  if (value.start !== undefined) {
    const fieldValue = value.start;
    if (!(fieldValue instanceof Uint8Array)) return false;
    if (fieldValue.length > 65536) return false;
  }
  if (value.end === undefined) return false;
  if (value.end !== undefined) {
    const fieldValue = value.end;
    if (!(fieldValue instanceof Uint8Array)) return false;
    if (fieldValue.length > 65536) return false;
  }
  if (value.escapeByte !== undefined) {
    const fieldValue = value.escapeByte;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue > 255) return false;
  }
  if (value.delimiter !== undefined) {
    const fieldValue = value.delimiter;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue > 255) return false;
  }
  if (value.header === undefined) return false;
  if (value.header !== undefined) {
    const fieldValue = value.header;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.frameBytes !== undefined) {
    const fieldValue = value.frameBytes;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
    if (fieldValue > 1048576) return false;
  }
  if (value.byteOrder === undefined) return false;
  if (value.byteOrder !== undefined) {
    const fieldValue = value.byteOrder;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.fields === undefined) return false;
  if (value.fields !== undefined) {
    const fieldValue = value.fields;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 64) return false;
    for (const item of fieldValue) {
    if (!checkFrameField(item, context)) return false;
    }
  }
  if (value.checksum !== undefined) {
    const fieldValue = value.checksum;
    if (!checkChecksumSpec(fieldValue, context)) return false;
  }
  if (!frameSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isFrameField(input: unknown): boolean { return checkFrameField(input, {active: new Set<object>(), depth: 0}); }
function checkFrameField(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.column !== undefined) {
    const fieldValue = value.column;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.jsonPath === undefined) return false;
  if (value.jsonPath !== undefined) {
    const fieldValue = value.jsonPath;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.scalarType === undefined) return false;
  if (value.scalarType !== undefined) {
    const fieldValue = value.scalarType;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "bool" && fieldValue !== "i8" && fieldValue !== "u8" && fieldValue !== "i16" && fieldValue !== "u16" && fieldValue !== "i32" && fieldValue !== "u32" && fieldValue !== "i64" && fieldValue !== "u64" && fieldValue !== "f32" && fieldValue !== "f64" && fieldValue !== "textNumber") return false;
  }
  if (value.required === undefined) return false;
  if (value.required !== undefined) {
    const fieldValue = value.required;
    if (typeof fieldValue !== "boolean") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isGeneratedSource(input: unknown): boolean { return checkGeneratedSource(input, {active: new Set<object>(), depth: 0}); }
function checkGeneratedSource(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "colour" && fieldValue !== "gradient" && fieldValue !== "counter" && fieldValue !== "testPattern" && fieldValue !== "title") return false;
  }
  if (value.parameters === undefined) return false;
  if (value.parameters !== undefined) {
    const fieldValue = value.parameters;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkEffectParameter(item, context)) return false;
    }
  }
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (!checkTimedText(fieldValue, context)) return false;
  }
  if ((value.kind === 'title') !== (value.text !== undefined) || (value.kind === 'title' && ((value.parameters ?? []) as unknown[]).length !== 0)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isIdList(input: unknown): boolean { return checkIdList(input, {active: new Set<object>(), depth: 0}); }
function checkIdList(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.items === undefined) return false;
  if (value.items !== undefined) {
    const fieldValue = value.items;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isImageLayout(input: unknown): boolean { return checkImageLayout(input, {active: new Set<object>(), depth: 0}); }
function checkImageLayout(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.widthRatio === undefined) return false;
  if (value.widthRatio !== undefined) {
    const fieldValue = value.widthRatio;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
    if (fieldValue < 0) return false;
    if (fieldValue > 1) return false;
  }
  if (value.alignment === undefined) return false;
  if (value.alignment !== undefined) {
    const fieldValue = value.alignment;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "start" && fieldValue !== "center" && fieldValue !== "end") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isInlineAtom(input: unknown): boolean { return checkInlineAtom(input, {active: new Set<object>(), depth: 0}); }
function checkInlineAtom(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.offset === undefined) return false;
  if (value.offset !== undefined) {
    const fieldValue = value.offset;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "mention") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkLinkSpec(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "math") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkMathContent(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "footnote") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkLinkSpec(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.inlineId === undefined) return false;
  if (value.inlineId !== undefined) {
    const fieldValue = value.inlineId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.content === undefined || typeof value.content !== 'object' || value.content === null) return false;
  if ((value.content as {case?: string}).case !== "mention" && (value.content as {case?: string}).case !== "math" && (value.content as {case?: string}).case !== "footnote") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isKeyframe(input: unknown): boolean { return checkKeyframe(input, {active: new Set<object>(), depth: 0}); }
function checkKeyframe(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.time === undefined) return false;
  if (value.time !== undefined) {
    const fieldValue = value.time;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if (value.interpolation === undefined) return false;
  if (value.interpolation !== undefined) {
    const fieldValue = value.interpolation;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "hold" && fieldValue !== "linear" && fieldValue !== "bezier") return false;
  }
  if (value.inTangent !== undefined) {
    const fieldValue = value.inTangent;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.outTangent !== undefined) {
    const fieldValue = value.outTangent;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.scope === undefined) return false;
  if (value.scope !== undefined) {
    const fieldValue = value.scope;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "clipLocal" && fieldValue !== "sequence") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isKeyframeList(input: unknown): boolean { return checkKeyframeList(input, {active: new Set<object>(), depth: 0}); }
function checkKeyframeList(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.items === undefined) return false;
  if (value.items !== undefined) {
    const fieldValue = value.items;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkKeyframe(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isLinkSpec(input: unknown): boolean { return checkLinkSpec(input, {active: new Set<object>(), depth: 0}); }
function checkLinkSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.targetId !== undefined) {
    const fieldValue = value.targetId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.url !== undefined) {
    const fieldValue = value.url;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.label !== undefined) {
    const fieldValue = value.label;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.targetId === undefined) === (value.url === undefined) || (value.url !== undefined && !safeLink(value.url as string))) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMarkerView(input: unknown): boolean { return checkMarkerView(input, {active: new Set<object>(), depth: 0}); }
function checkMarkerView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.markerId === undefined) return false;
  if (value.markerId !== undefined) {
    const fieldValue = value.markerId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.sequenceId === undefined) return false;
  if (value.sequenceId !== undefined) {
    const fieldValue = value.sequenceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.range === undefined) return false;
  if (value.range !== undefined) {
    const fieldValue = value.range;
    if (!checkMediaRange(fieldValue, context)) return false;
  }
  if (value.label === undefined) return false;
  if (value.label !== undefined) {
    const fieldValue = value.label;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.clipId !== undefined) {
    const fieldValue = value.clipId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.anchor === undefined) return false;
  if (value.anchor !== undefined) {
    const fieldValue = value.anchor;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "sequence" && fieldValue !== "clipLocal" && fieldValue !== "source") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMathContent(input: unknown): boolean { return checkMathContent(input, {active: new Set<object>(), depth: 0}); }
function checkMathContent(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.tex === undefined) return false;
  if (value.tex !== undefined) {
    const fieldValue = value.tex;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 4096) return false;
  }
  if (value.display === undefined) return false;
  if (value.display !== undefined) {
    const fieldValue = value.display;
    if (typeof fieldValue !== "boolean") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementRequest(input: unknown): boolean { return checkMeasurementRequest(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementRequest(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "scope.measurement.v1") return false;
  }
  if (value.source === undefined) return false;
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkMeasurementSource(fieldValue, context)) return false;
  }
  if (value.sourceRevision === undefined) return false;
  if (value.sourceRevision !== undefined) {
    const fieldValue = value.sourceRevision;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if (value.channels === undefined) return false;
  if (value.channels !== undefined) {
    const fieldValue = value.channels;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 1) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.window === undefined) return false;
  if (value.window !== undefined) {
    const fieldValue = value.window;
    if (!checkMeasurementWindow(fieldValue, context)) return false;
  }
  if (value.families === undefined) return false;
  if (value.families !== undefined) {
    const fieldValue = value.families;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 1) return false;
    if (fieldValue.length > 14) return false;
    if (new Set(fieldValue).size !== fieldValue.length) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    if (item !== "minimum" && item !== "maximum" && item !== "mean" && item !== "rms" && item !== "peakToPeak" && item !== "standardDeviation" && item !== "count" && item !== "duration" && item !== "frequency" && item !== "dutyCycle" && item !== "riseTime" && item !== "fallTime" && item !== "cursorDelta" && item !== "eventCount") return false;
    }
  }
  if (value.thresholds === undefined) return false;
  if (value.thresholds !== undefined) {
    const fieldValue = value.thresholds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMeasurementThreshold(item, context)) return false;
    }
  }
  if (value.configuration === undefined) return false;
  if (value.configuration !== undefined) {
    const fieldValue = value.configuration;
    if (!checkScopeConfiguration(fieldValue, context)) return false;
  }
  if (value.alignment === undefined) return false;
  if (value.alignment !== undefined) {
    const fieldValue = value.alignment;
    if (!checkAlignmentSpec(fieldValue, context)) return false;
  }
  if (value.cursors === undefined) return false;
  if (value.cursors !== undefined) {
    const fieldValue = value.cursors;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkCursorSpec(item, context)) return false;
    }
  }
  if (value.referenceLevels === undefined) return false;
  if (value.referenceLevels !== undefined) {
    const fieldValue = value.referenceLevels;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMeasurementThreshold(item, context)) return false;
    }
  }
  if (!measurementRequestSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementResult(input: unknown): boolean { return checkMeasurementResult(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementResult(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "scope.measurement.v1") return false;
  }
  if (value.inputHash === undefined) return false;
  if (value.inputHash !== undefined) {
    const fieldValue = value.inputHash;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.families === undefined) return false;
  if (value.families !== undefined) {
    const fieldValue = value.families;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkFamilyResult(item, context)) return false;
    }
  }
  if (value.warnings === undefined) return false;
  if (value.warnings !== undefined) {
    const fieldValue = value.warnings;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    }
  }
  if (value.configuration === undefined) return false;
  if (value.configuration !== undefined) {
    const fieldValue = value.configuration;
    if (!checkScopeConfiguration(fieldValue, context)) return false;
  }
  if (value.finiteCount === undefined) return false;
  if (value.finiteCount !== undefined) {
    const fieldValue = value.finiteCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.excludedCount === undefined) return false;
  if (value.excludedCount !== undefined) {
    const fieldValue = value.excludedCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.runCount === undefined) return false;
  if (value.runCount !== undefined) {
    const fieldValue = value.runCount;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.requestedDuration === undefined) return false;
  if (value.requestedDuration !== undefined) {
    const fieldValue = value.requestedDuration;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.coveredDuration === undefined) return false;
  if (value.coveredDuration !== undefined) {
    const fieldValue = value.coveredDuration;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.timingUncertainty === undefined) return false;
  if (value.timingUncertainty !== undefined) {
    const fieldValue = value.timingUncertainty;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.source === undefined) return false;
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkMeasurementSource(fieldValue, context)) return false;
  }
  if (value.resolvedThresholds === undefined) return false;
  if (value.resolvedThresholds !== undefined) {
    const fieldValue = value.resolvedThresholds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMeasurementThreshold(item, context)) return false;
    }
  }
  if (value.cursor !== undefined) {
    const fieldValue = value.cursor;
    if (!checkCursorResult(fieldValue, context)) return false;
  }
  if (!measurementResultSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementSource(input: unknown): boolean { return checkMeasurementSource(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementSource(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.capture === undefined) return false;
  if (value.capture !== undefined) {
    const fieldValue = value.capture;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.committedPrefix === undefined) return false;
  if (value.committedPrefix !== undefined) {
    const fieldValue = value.committedPrefix;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.configuration === undefined) return false;
  if (value.configuration !== undefined) {
    const fieldValue = value.configuration;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.decoder !== undefined) {
    const fieldValue = value.decoder;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.timeMapping !== undefined) {
    const fieldValue = value.timeMapping;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.eventSet !== undefined) {
    const fieldValue = value.eventSet;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementThreshold(input: unknown): boolean { return checkMeasurementThreshold(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementThreshold(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.family === undefined) return false;
  if (value.family !== undefined) {
    const fieldValue = value.family;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "frequency" && fieldValue !== "dutyCycle" && fieldValue !== "riseTime" && fieldValue !== "fallTime") return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "low" && fieldValue !== "high" && fieldValue !== "fraction10" && fieldValue !== "fraction50" && fieldValue !== "fraction90") return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.unit === undefined) return false;
  if (value.unit !== undefined) {
    const fieldValue = value.unit;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (!thresholdSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementValue(input: unknown): boolean { return checkMeasurementValue(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementValue(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "minimum" && fieldValue !== "maximum" && fieldValue !== "mean" && fieldValue !== "rms" && fieldValue !== "peakToPeak" && fieldValue !== "standardDeviation" && fieldValue !== "count" && fieldValue !== "duration" && fieldValue !== "frequency" && fieldValue !== "dutyCycle" && fieldValue !== "riseTime" && fieldValue !== "fallTime" && fieldValue !== "cursorDelta" && fieldValue !== "eventCount" && fieldValue !== "deltaTime" && fieldValue !== "deltaValue") return false;
  }
  if ((value.result as {case?: string} | undefined)?.case === "value") {
    const fieldValue = (value.result as {value: unknown}).value;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.unit === undefined) return false;
  if (value.unit !== undefined) {
    const fieldValue = value.unit;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if ((value.result as {case?: string} | undefined)?.case === "count") {
    const fieldValue = (value.result as {value: unknown}).value;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.result === undefined || typeof value.result !== 'object' || value.result === null) return false;
  if ((value.result as {case?: string}).case !== "value" && (value.result as {case?: string}).case !== "count") return false;
  if (['count','eventCount'].includes(value.name as string) !== ((value.result as Profile).case === 'count') || ((value.result as Profile).case === 'count' && value.unit !== '1')) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMeasurementWindow(input: unknown): boolean { return checkMeasurementWindow(input, {active: new Set<object>(), depth: 0}); }
function checkMeasurementWindow(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.start === undefined) return false;
  if (value.start !== undefined) {
    const fieldValue = value.start;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.end === undefined) return false;
  if (value.end !== undefined) {
    const fieldValue = value.end;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (compareScopeTime(value.start as Profile, value.end as Profile) >= 0) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaBin(input: unknown): boolean { return checkMediaBin(input, {active: new Set<object>(), depth: 0}); }
function checkMediaBin(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.binId === undefined) return false;
  if (value.binId !== undefined) {
    const fieldValue = value.binId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.parentId !== undefined) {
    const fieldValue = value.parentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.mediaIds === undefined) return false;
  if (value.mediaIds !== undefined) {
    const fieldValue = value.mediaIds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.sequenceIds === undefined) return false;
  if (value.sequenceIds !== undefined) {
    const fieldValue = value.sequenceIds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.order === undefined) return false;
  if (value.order !== undefined) {
    const fieldValue = value.order;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaColourAssignment(input: unknown): boolean { return checkMediaColourAssignment(input, {active: new Set<object>(), depth: 0}); }
function checkMediaColourAssignment(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.mediaId === undefined) return false;
  if (value.mediaId !== undefined) {
    const fieldValue = value.mediaId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.inputSpace === undefined) return false;
  if (value.inputSpace !== undefined) {
    const fieldValue = value.inputSpace;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaStream(input: unknown): boolean { return checkMediaStream(input, {active: new Set<object>(), depth: 0}); }
function checkMediaStream(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.index === undefined) return false;
  if (value.index !== undefined) {
    const fieldValue = value.index;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.codec === undefined) return false;
  if (value.codec !== undefined) {
    const fieldValue = value.codec;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.rate !== undefined) {
    const fieldValue = value.rate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.channels !== undefined) {
    const fieldValue = value.channels;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMediaView(input: unknown): boolean { return checkMediaView(input, {active: new Set<object>(), depth: 0}); }
function checkMediaView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.mediaId === undefined) return false;
  if (value.mediaId !== undefined) {
    const fieldValue = value.mediaId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if (value.availability === undefined) return false;
  if (value.availability !== undefined) {
    const fieldValue = value.availability;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4 && fieldValue !== 5) return false;
  }
  if (value.duration !== undefined) {
    const fieldValue = value.duration;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.streams === undefined) return false;
  if (value.streams !== undefined) {
    const fieldValue = value.streams;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMediaStream(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMemoryRecord(input: unknown): boolean { return checkMemoryRecord(input, {active: new Set<object>(), depth: 0}); }
function checkMemoryRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.memoryId === undefined) return false;
  if (value.memoryId !== undefined) {
    const fieldValue = value.memoryId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.sources === undefined) return false;
  if (value.sources !== undefined) {
    const fieldValue = value.sources;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMessageDraft(input: unknown): boolean { return checkMessageDraft(input, {active: new Set<object>(), depth: 0}); }
function checkMessageDraft(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.parts === undefined) return false;
  if (value.parts !== undefined) {
    const fieldValue = value.parts;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMessagePart(item, context)) return false;
    }
  }
  if (value.context === undefined) return false;
  if (value.context !== undefined) {
    const fieldValue = value.context;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMessagePart(input: unknown): boolean { return checkMessagePart(input, {active: new Set<object>(), depth: 0}); }
function checkMessagePart(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.content as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "resource") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "toolProposal") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkToolProposal(fieldValue, context)) return false;
  }
  if ((value.content as {case?: string} | undefined)?.case === "toolResult") {
    const fieldValue = (value.content as {value: unknown}).value;
    if (!checkToolResult(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.content === undefined || typeof value.content !== 'object' || value.content === null) return false;
  if ((value.content as {case?: string}).case !== "text" && (value.content as {case?: string}).case !== "resource" && (value.content as {case?: string}).case !== "toolProposal" && (value.content as {case?: string}).case !== "toolResult") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMessageView(input: unknown): boolean { return checkMessageView(input, {active: new Set<object>(), depth: 0}); }
function checkMessageView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.messageId === undefined) return false;
  if (value.messageId !== undefined) {
    const fieldValue = value.messageId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.conversationId === undefined) return false;
  if (value.conversationId !== undefined) {
    const fieldValue = value.conversationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.branchId === undefined) return false;
  if (value.branchId !== undefined) {
    const fieldValue = value.branchId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.role === undefined) return false;
  if (value.role !== undefined) {
    const fieldValue = value.role;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "user" && fieldValue !== "assistant" && fieldValue !== "tool" && fieldValue !== "system") return false;
  }
  if (value.parts === undefined) return false;
  if (value.parts !== undefined) {
    const fieldValue = value.parts;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMessagePart(item, context)) return false;
    }
  }
  if (value.state === undefined) return false;
  if (value.state !== undefined) {
    const fieldValue = value.state;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "pending" && fieldValue !== "complete" && fieldValue !== "interrupted" && fieldValue !== "failed") return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.taskId !== undefined) {
    const fieldValue = value.taskId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.turnId !== undefined) {
    const fieldValue = value.turnId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.taskId !== undefined && value.turnId !== undefined) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMetadataEntry(input: unknown): boolean { return checkMetadataEntry(input, {active: new Set<object>(), depth: 0}); }
function checkMetadataEntry(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkMetadataScalar(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isMetadataScalar(input: unknown): boolean { return checkMetadataScalar(input, {active: new Set<object>(), depth: 0}); }
function checkMetadataScalar(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.value as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "boolean") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "integer") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "number") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "decimal") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "instant") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "resource") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if (value.value === undefined || typeof value.value !== 'object' || value.value === null) return false;
  if ((value.value as {case?: string}).case !== "text" && (value.value as {case?: string}).case !== "boolean" && (value.value as {case?: string}).case !== "integer" && (value.value as {case?: string}).case !== "number" && (value.value as {case?: string}).case !== "decimal" && (value.value as {case?: string}).case !== "instant" && (value.value as {case?: string}).case !== "resource") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotebookBody(input: unknown): boolean { return checkNotebookBody(input, {active: new Set<object>(), depth: 0}); }
function checkNotebookBody(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.notebook === undefined) return false;
  if (value.notebook !== undefined) {
    const fieldValue = value.notebook;
    if (!checkNotebookView(fieldValue, context)) return false;
  }
  if (value.folders === undefined) return false;
  if (value.folders !== undefined) {
    const fieldValue = value.folders;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkFolderView(item, context)) return false;
    }
  }
  if (value.documentOrder === undefined) return false;
  if (value.documentOrder !== undefined) {
    const fieldValue = value.documentOrder;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotebookView(input: unknown): boolean { return checkNotebookView(input, {active: new Set<object>(), depth: 0}); }
function checkNotebookView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesDocument(input: unknown): boolean { return checkNotesDocument(input, {active: new Set<object>(), depth: 0}); }
function checkNotesDocument(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.documentId === undefined) return false;
  if (value.documentId !== undefined) {
    const fieldValue = value.documentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.folderId !== undefined) {
    const fieldValue = value.folderId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.title === undefined) return false;
  if (value.title !== undefined) {
    const fieldValue = value.title;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.blocks === undefined) return false;
  if (value.blocks !== undefined) {
    const fieldValue = value.blocks;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkBlock(item, context)) return false;
    }
  }
  if (value.properties === undefined) return false;
  if (value.properties !== undefined) {
    const fieldValue = value.properties;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkPropertyValue(item, context)) return false;
    }
  }
  if (value.tags === undefined) return false;
  if (value.tags !== undefined) {
    const fieldValue = value.tags;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.links === undefined) return false;
  if (value.links !== undefined) {
    const fieldValue = value.links;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkLinkSpec(item, context)) return false;
    }
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (!documentSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesFilter(input: unknown): boolean { return checkNotesFilter(input, {active: new Set<object>(), depth: 0}); }
function checkNotesFilter(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.expression as {case?: string} | undefined)?.case === "group") {
    const fieldValue = (value.expression as {value: unknown}).value;
    if (!checkFilterGroup(fieldValue, context)) return false;
  }
  if ((value.expression as {case?: string} | undefined)?.case === "predicate") {
    const fieldValue = (value.expression as {value: unknown}).value;
    if (!checkScalarPredicate(fieldValue, context)) return false;
  }
  if (value.expression === undefined || typeof value.expression !== 'object' || value.expression === null) return false;
  if ((value.expression as {case?: string}).case !== "group" && (value.expression as {case?: string}).case !== "predicate") return false;
  if (!filterBounds(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesQuery(input: unknown): boolean { return checkNotesQuery(input, {active: new Set<object>(), depth: 0}); }
function checkNotesQuery(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "notes.scalar.v1") return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.savedViewId !== undefined) {
    const fieldValue = value.savedViewId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.savedViewRev !== undefined) {
    const fieldValue = value.savedViewRev;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.filter !== undefined) {
    const fieldValue = value.filter;
    if (!checkNotesFilter(fieldValue, context)) return false;
  }
  if (value.sorts === undefined) return false;
  if (value.sorts !== undefined) {
    const fieldValue = value.sorts;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 8) return false;
    for (const item of fieldValue) {
    if (!checkNotesSort(item, context)) return false;
    }
  }
  if (value.projection === undefined) return false;
  if (value.projection !== undefined) {
    const fieldValue = value.projection;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 64) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.page === undefined) return false;
  if (value.page !== undefined) {
    const fieldValue = value.page;
    if (!checkPageRequest(fieldValue, context)) return false;
  }
  if (value.datasetToken !== undefined) {
    const fieldValue = value.datasetToken;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 4096) return false;
  }
  if (value.definitionVersions === undefined) return false;
  if (value.definitionVersions !== undefined) {
    const fieldValue = value.definitionVersions;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkPropertyDefinitionVersion(item, context)) return false;
    }
  }
  if (value.selectors !== undefined) {
    const fieldValue = value.selectors;
    if (!checkNotesSelectors(fieldValue, context)) return false;
  }
  if (!querySemantics(value) || toBinary(NotesQuerySchema, value as never).length > 65536) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesSelection(input: unknown): boolean { return checkNotesSelection(input, {active: new Set<object>(), depth: 0}); }
function checkNotesSelection(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.documentId === undefined) return false;
  if (value.documentId !== undefined) {
    const fieldValue = value.documentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.anchor === undefined) return false;
  if (value.anchor !== undefined) {
    const fieldValue = value.anchor;
    if (!checkNotesTextPosition(fieldValue, context)) return false;
  }
  if (value.focus === undefined) return false;
  if (value.focus !== undefined) {
    const fieldValue = value.focus;
    if (!checkNotesTextPosition(fieldValue, context)) return false;
  }
  if (value.localVersion !== undefined) {
    const fieldValue = value.localVersion;
    if (!checkLocalNotesVersion(fieldValue, context)) return false;
  }
  if (value.cloudRevision !== undefined) {
    const fieldValue = value.cloudRevision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if ((value.localVersion === undefined) === (value.cloudRevision === undefined) || (value.cloudRevision !== undefined && (value.cloudRevision as Profile).value <= 0n)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesSelectors(input: unknown): boolean { return checkNotesSelectors(input, {active: new Set<object>(), depth: 0}); }
function checkNotesSelectors(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.documentId !== undefined) {
    const fieldValue = value.documentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.folderId !== undefined) {
    const fieldValue = value.folderId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.tagIds === undefined) return false;
  if (value.tagIds !== undefined) {
    const fieldValue = value.tagIds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.blockKinds === undefined) return false;
  if (value.blockKinds !== undefined) {
    const fieldValue = value.blockKinds;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  if (value.hasAttachment !== undefined) {
    const fieldValue = value.hasAttachment;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.modified !== undefined) {
    const fieldValue = value.modified;
    if (!checkTimeRangeUtc(fieldValue, context)) return false;
  }
  if (value.linkedTo !== undefined) {
    const fieldValue = value.linkedTo;
    if (!checkId(fieldValue, context)) return false;
  }
  if (!uniqueIds((value.tagIds ?? []) as Profile[]) || new Set((value.blockKinds ?? []) as string[]).size !== ((value.blockKinds ?? []) as string[]).length) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesSort(input: unknown): boolean { return checkNotesSort(input, {active: new Set<object>(), depth: 0}); }
function checkNotesSort(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.propertyId === undefined) return false;
  if (value.propertyId !== undefined) {
    const fieldValue = value.propertyId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.descending === undefined) return false;
  if (value.descending !== undefined) {
    const fieldValue = value.descending;
    if (typeof fieldValue !== "boolean") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isNotesTextPosition(input: unknown): boolean { return checkNotesTextPosition(input, {active: new Set<object>(), depth: 0}); }
function checkNotesTextPosition(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.blockId === undefined) return false;
  if (value.blockId !== undefined) {
    const fieldValue = value.blockId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.inlineId === undefined) return false;
  if (value.inlineId !== undefined) {
    const fieldValue = value.inlineId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.offsetUtf16 === undefined) return false;
  if (value.offsetUtf16 !== undefined) {
    const fieldValue = value.offsetUtf16;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.affinity === undefined) return false;
  if (value.affinity !== undefined) {
    const fieldValue = value.affinity;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "before" && fieldValue !== "after") return false;
  }
  if (value.cellId !== undefined) {
    const fieldValue = value.cellId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPreferenceRecord(input: unknown): boolean { return checkPreferenceRecord(input, {active: new Set<object>(), depth: 0}); }
function checkPreferenceRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.preferenceId === undefined) return false;
  if (value.preferenceId !== undefined) {
    const fieldValue = value.preferenceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.ownerAppId === undefined) return false;
  if (value.ownerAppId !== undefined) {
    const fieldValue = value.ownerAppId;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.values === undefined) return false;
  if (value.values !== undefined) {
    const fieldValue = value.values;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 200) return false;
    for (const item of fieldValue) {
    if (!checkMetadataEntry(item, context)) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isProcessingEdge(input: unknown): boolean { return checkProcessingEdge(input, {active: new Set<object>(), depth: 0}); }
function checkProcessingEdge(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.sourceNodeId === undefined) return false;
  if (value.sourceNodeId !== undefined) {
    const fieldValue = value.sourceNodeId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.sourcePort === undefined) return false;
  if (value.sourcePort !== undefined) {
    const fieldValue = value.sourcePort;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.targetNodeId === undefined) return false;
  if (value.targetNodeId !== undefined) {
    const fieldValue = value.targetNodeId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.targetPort === undefined) return false;
  if (value.targetPort !== undefined) {
    const fieldValue = value.targetPort;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isProcessingGraph(input: unknown): boolean { return checkProcessingGraph(input, {active: new Set<object>(), depth: 0}); }
function checkProcessingGraph(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.nodes === undefined) return false;
  if (value.nodes !== undefined) {
    const fieldValue = value.nodes;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkEffectSpec(item, context)) return false;
    }
  }
  if (value.edges === undefined) return false;
  if (value.edges !== undefined) {
    const fieldValue = value.edges;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkProcessingEdge(item, context)) return false;
    }
  }
  if (value.outputNodeId !== undefined) {
    const fieldValue = value.outputNodeId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "slate.graph.v1") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPropertyDefinition(input: unknown): boolean { return checkPropertyDefinition(input, {active: new Set<object>(), depth: 0}); }
function checkPropertyDefinition(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.propertyId === undefined) return false;
  if (value.propertyId !== undefined) {
    const fieldValue = value.propertyId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.type === undefined) return false;
  if (value.type !== undefined) {
    const fieldValue = value.type;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "text" && fieldValue !== "url" && fieldValue !== "number" && fieldValue !== "date" && fieldValue !== "dateTime" && fieldValue !== "checkbox" && fieldValue !== "select" && fieldValue !== "multiSelect") return false;
  }
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "notes.scalar.v1") return false;
  }
  if (value.semanticRevision === undefined) return false;
  if (value.semanticRevision !== undefined) {
    const fieldValue = value.semanticRevision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.options === undefined) return false;
  if (value.options !== undefined) {
    const fieldValue = value.options;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 1000) return false;
    for (const item of fieldValue) {
    if (!checkSelectOption(item, context)) return false;
    }
  }
  if (value.numberScale !== undefined) {
    const fieldValue = value.numberScale;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue > 9) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (!definitionSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPropertyDefinitionVersion(input: unknown): boolean { return checkPropertyDefinitionVersion(input, {active: new Set<object>(), depth: 0}); }
function checkPropertyDefinitionVersion(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.propertyId === undefined) return false;
  if (value.propertyId !== undefined) {
    const fieldValue = value.propertyId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.semanticRevision === undefined) return false;
  if (value.semanticRevision !== undefined) {
    const fieldValue = value.semanticRevision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isPropertyValue(input: unknown): boolean { return checkPropertyValue(input, {active: new Set<object>(), depth: 0}); }
function checkPropertyValue(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.propertyId === undefined) return false;
  if (value.propertyId !== undefined) {
    const fieldValue = value.propertyId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkScalarValue(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRenderPreset(input: unknown): boolean { return checkRenderPreset(input, {active: new Set<object>(), depth: 0}); }
function checkRenderPreset(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.container === undefined) return false;
  if (value.container !== undefined) {
    const fieldValue = value.container;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.videoCodec !== undefined) {
    const fieldValue = value.videoCodec;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.audioCodec !== undefined) {
    const fieldValue = value.audioCodec;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.width !== undefined) {
    const fieldValue = value.width;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.height !== undefined) {
    const fieldValue = value.height;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.rate !== undefined) {
    const fieldValue = value.rate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.colourSpace === undefined) return false;
  if (value.colourSpace !== undefined) {
    const fieldValue = value.colourSpace;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.sampleRate !== undefined) {
    const fieldValue = value.sampleRate;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.channelLayout !== undefined) {
    const fieldValue = value.channelLayout;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.audioBitrate !== undefined) {
    const fieldValue = value.audioBitrate;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.videoBitrate !== undefined) {
    const fieldValue = value.videoBitrate;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.subtitleMode === undefined) return false;
  if (value.subtitleMode !== undefined) {
    const fieldValue = value.subtitleMode;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "none" && fieldValue !== "burnIn" && fieldValue !== "sidecar") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRetimeCurve(input: unknown): boolean { return checkRetimeCurve(input, {active: new Set<object>(), depth: 0}); }
function checkRetimeCurve(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.points === undefined) return false;
  if (value.points !== undefined) {
    const fieldValue = value.points;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 2) return false;
    for (const item of fieldValue) {
    if (!checkRetimePoint(item, context)) return false;
    }
  }
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "slate.retime.v1") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRetimePoint(input: unknown): boolean { return checkRetimePoint(input, {active: new Set<object>(), depth: 0}); }
function checkRetimePoint(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.timelineOffset === undefined) return false;
  if (value.timelineOffset !== undefined) {
    const fieldValue = value.timelineOffset;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.sourceOffset === undefined) return false;
  if (value.sourceOffset !== undefined) {
    const fieldValue = value.sourceOffset;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isRichText(input: unknown): boolean { return checkRichText(input, {active: new Set<object>(), depth: 0}); }
function checkRichText(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.spans === undefined) return false;
  if (value.spans !== undefined) {
    const fieldValue = value.spans;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTextSpan(item, context)) return false;
    }
  }
  if (value.atoms === undefined) return false;
  if (value.atoms !== undefined) {
    const fieldValue = value.atoms;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkInlineAtom(item, context)) return false;
    }
  }
  if (value.runs === undefined) return false;
  if (value.runs !== undefined) {
    const fieldValue = value.runs;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTextRunSegment(item, context)) return false;
    }
  }
  if (!richTextSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSampleRange(input: unknown): boolean { return checkSampleRange(input, {active: new Set<object>(), depth: 0}); }
function checkSampleRange(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.from === undefined) return false;
  if (value.from !== undefined) {
    const fieldValue = value.from;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.count === undefined) return false;
  if (value.count !== undefined) {
    const fieldValue = value.count;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if ((value.from as bigint) + (value.count as bigint) > 18446744073709551615n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSavedViewRecord(input: unknown): boolean { return checkSavedViewRecord(input, {active: new Set<object>(), depth: 0}); }
function checkSavedViewRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.viewId === undefined) return false;
  if (value.viewId !== undefined) {
    const fieldValue = value.viewId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.layout === undefined) return false;
  if (value.layout !== undefined) {
    const fieldValue = value.layout;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "list" && fieldValue !== "table") return false;
  }
  if (value.query === undefined) return false;
  if (value.query !== undefined) {
    const fieldValue = value.query;
    if (!checkNotesQuery(fieldValue, context)) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (!savedViewSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScalarPredicate(input: unknown): boolean { return checkScalarPredicate(input, {active: new Set<object>(), depth: 0}); }
function checkScalarPredicate(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.propertyId === undefined) return false;
  if (value.propertyId !== undefined) {
    const fieldValue = value.propertyId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.operator === undefined) return false;
  if (value.operator !== undefined) {
    const fieldValue = value.operator;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "eq" && fieldValue !== "ne" && fieldValue !== "lt" && fieldValue !== "le" && fieldValue !== "gt" && fieldValue !== "ge" && fieldValue !== "in" && fieldValue !== "contains" && fieldValue !== "startsWith" && fieldValue !== "endsWith" && fieldValue !== "hasAny" && fieldValue !== "hasAll" && fieldValue !== "isMissing" && fieldValue !== "isPresent") return false;
  }
  if (value.operands === undefined) return false;
  if (value.operands !== undefined) {
    const fieldValue = value.operands;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 100) return false;
    for (const item of fieldValue) {
    if (!checkScalarValue(item, context)) return false;
    }
  }
  if (!predicateSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScalarValue(input: unknown): boolean { return checkScalarValue(input, {active: new Set<object>(), depth: 0}); }
function checkScalarValue(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.value as {case?: string} | undefined)?.case === "null") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "boolean") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "number") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "date") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "select") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkId(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "multiSelect") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkIdList(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "dateTime") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "url") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.value === undefined || typeof value.value !== 'object' || value.value === null) return false;
  if ((value.value as {case?: string}).case !== "null" && (value.value as {case?: string}).case !== "text" && (value.value as {case?: string}).case !== "boolean" && (value.value as {case?: string}).case !== "number" && (value.value as {case?: string}).case !== "date" && (value.value as {case?: string}).case !== "select" && (value.value as {case?: string}).case !== "multiSelect" && (value.value as {case?: string}).case !== "dateTime" && (value.value as {case?: string}).case !== "url") return false;
  if (!scalarSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScheduleSpec(input: unknown): boolean { return checkScheduleSpec(input, {active: new Set<object>(), depth: 0}); }
function checkScheduleSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "once" && fieldValue !== "cron" && fieldValue !== "interval") return false;
  }
  if (value.expression === undefined) return false;
  if (value.expression !== undefined) {
    const fieldValue = value.expression;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.zone === undefined) return false;
  if (value.zone !== undefined) {
    const fieldValue = value.zone;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.startsAt === undefined) return false;
  if (value.startsAt !== undefined) {
    const fieldValue = value.startsAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.endsAt !== undefined) {
    const fieldValue = value.endsAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.dstGap === undefined) return false;
  if (value.dstGap !== undefined) {
    const fieldValue = value.dstGap;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "skip") return false;
  }
  if (value.dstOverlap === undefined) return false;
  if (value.dstOverlap !== undefined) {
    const fieldValue = value.dstOverlap;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "earlier" && fieldValue !== "later" && fieldValue !== "both") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeAnnotation(input: unknown): boolean { return checkScopeAnnotation(input, {active: new Set<object>(), depth: 0}); }
function checkScopeAnnotation(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.annotationId === undefined) return false;
  if (value.annotationId !== undefined) {
    const fieldValue = value.annotationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.range === undefined) return false;
  if (value.range !== undefined) {
    const fieldValue = value.range;
    if (!checkSampleRange(fieldValue, context)) return false;
  }
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeConfiguration(input: unknown): boolean { return checkScopeConfiguration(input, {active: new Set<object>(), depth: 0}); }
function checkScopeConfiguration(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.configurationId === undefined) return false;
  if (value.configurationId !== undefined) {
    const fieldValue = value.configurationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.channels === undefined) return false;
  if (value.channels !== undefined) {
    const fieldValue = value.channels;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length < 1) return false;
    for (const item of fieldValue) {
    if (!checkChannelDefinition(item, context)) return false;
    }
  }
  if (value.parserProfile === undefined) return false;
  if (value.parserProfile !== undefined) {
    const fieldValue = value.parserProfile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if (value.framing === undefined) return false;
  if (value.framing !== undefined) {
    const fieldValue = value.framing;
    if (!checkFrameConfiguration(fieldValue, context)) return false;
  }
  if (value.trigger !== undefined) {
    const fieldValue = value.trigger;
    if (!checkTriggerConfiguration(fieldValue, context)) return false;
  }
  if (!configurationSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeFinding(input: unknown): boolean { return checkScopeFinding(input, {active: new Set<object>(), depth: 0}); }
function checkScopeFinding(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.findingId === undefined) return false;
  if (value.findingId !== undefined) {
    const fieldValue = value.findingId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.result === undefined) return false;
  if (value.result !== undefined) {
    const fieldValue = value.result;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.severity === undefined) return false;
  if (value.severity !== undefined) {
    const fieldValue = value.severity;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "info" && fieldValue !== "warning" && fieldValue !== "critical") return false;
  }
  if (value.status === undefined) return false;
  if (value.status !== undefined) {
    const fieldValue = value.status;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "open" && fieldValue !== "accepted" && fieldValue !== "dismissed") return false;
  }
  if (value.evidence === undefined) return false;
  if (value.evidence !== undefined) {
    const fieldValue = value.evidence;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  if (value.actor === undefined) return false;
  if (value.actor !== undefined) {
    const fieldValue = value.actor;
    if (!checkActorChain(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeMetadata(input: unknown): boolean { return checkScopeMetadata(input, {active: new Set<object>(), depth: 0}); }
function checkScopeMetadata(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.sessionId === undefined) return false;
  if (value.sessionId !== undefined) {
    const fieldValue = value.sessionId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.captures === undefined) return false;
  if (value.captures !== undefined) {
    const fieldValue = value.captures;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkCaptureMetadata(item, context)) return false;
    }
  }
  if (value.annotations === undefined) return false;
  if (value.annotations !== undefined) {
    const fieldValue = value.annotations;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkScopeAnnotation(item, context)) return false;
    }
  }
  if (value.findings === undefined) return false;
  if (value.findings !== undefined) {
    const fieldValue = value.findings;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkScopeFinding(item, context)) return false;
    }
  }
  if (value.projectId === undefined) return false;
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.configuration === undefined) return false;
  if (value.configuration !== undefined) {
    const fieldValue = value.configuration;
    if (!checkScopeConfiguration(fieldValue, context)) return false;
  }
  if (value.analyses === undefined) return false;
  if (value.analyses !== undefined) {
    const fieldValue = value.analyses;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkResourceVersionRef(item, context)) return false;
    }
  }
  if (value.reports === undefined) return false;
  if (value.reports !== undefined) {
    const fieldValue = value.reports;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkArtifactRef(item, context)) return false;
    }
  }
  if (value.tags === undefined) return false;
  if (value.tags !== undefined) {
    const fieldValue = value.tags;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.metadata === undefined) return false;
  if (value.metadata !== undefined) {
    const fieldValue = value.metadata;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 200) return false;
    for (const item of fieldValue) {
    if (!checkMetadataEntry(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeSelection(input: unknown): boolean { return checkScopeSelection(input, {active: new Set<object>(), depth: 0}); }
function checkScopeSelection(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.sessionId === undefined) return false;
  if (value.sessionId !== undefined) {
    const fieldValue = value.sessionId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.captureId === undefined) return false;
  if (value.captureId !== undefined) {
    const fieldValue = value.captureId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.channels === undefined) return false;
  if (value.channels !== undefined) {
    const fieldValue = value.channels;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 64) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.range === undefined) return false;
  if (value.range !== undefined) {
    const fieldValue = value.range;
    if (!checkMediaRange(fieldValue, context)) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isScopeTime(input: unknown): boolean { return checkScopeTime(input, {active: new Set<object>(), depth: 0}); }
function checkScopeTime(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.ticks === undefined) return false;
  if (value.ticks !== undefined) {
    const fieldValue = value.ticks;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if (value.rate === undefined) return false;
  if (value.rate !== undefined) {
    const fieldValue = value.rate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if ((value.rate as Profile).numerator <= 0n) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSelectOption(input: unknown): boolean { return checkSelectOption(input, {active: new Set<object>(), depth: 0}); }
function checkSelectOption(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.optionId === undefined) return false;
  if (value.optionId !== undefined) {
    const fieldValue = value.optionId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.label === undefined) return false;
  if (value.label !== undefined) {
    const fieldValue = value.label;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.order === undefined) return false;
  if (value.order !== undefined) {
    const fieldValue = value.order;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSelectedSample(input: unknown): boolean { return checkSelectedSample(input, {active: new Set<object>(), depth: 0}); }
function checkSelectedSample(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.captureId === undefined) return false;
  if (value.captureId !== undefined) {
    const fieldValue = value.captureId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.channelId === undefined) return false;
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.segmentId === undefined) return false;
  if (value.segmentId !== undefined) {
    const fieldValue = value.segmentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.sampleIndex === undefined) return false;
  if (value.sampleIndex !== undefined) {
    const fieldValue = value.sampleIndex;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < 0n || fieldValue > 18446744073709551615n) return false;
  }
  if (value.time === undefined) return false;
  if (value.time !== undefined) {
    const fieldValue = value.time;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSequenceView(input: unknown): boolean { return checkSequenceView(input, {active: new Set<object>(), depth: 0}); }
function checkSequenceView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.sequenceId === undefined) return false;
  if (value.sequenceId !== undefined) {
    const fieldValue = value.sequenceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.projectId === undefined) return false;
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if (value.duration === undefined) return false;
  if (value.duration !== undefined) {
    const fieldValue = value.duration;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.videoRate === undefined) return false;
  if (value.videoRate !== undefined) {
    const fieldValue = value.videoRate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.sampleRate === undefined) return false;
  if (value.sampleRate !== undefined) {
    const fieldValue = value.sampleRate;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
  }
  if (value.width === undefined) return false;
  if (value.width !== undefined) {
    const fieldValue = value.width;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
  }
  if (value.height === undefined) return false;
  if (value.height !== undefined) {
    const fieldValue = value.height;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
  }
  if (value.channelLayout === undefined) return false;
  if (value.channelLayout !== undefined) {
    const fieldValue = value.channelLayout;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.colour === undefined) return false;
  if (value.colour !== undefined) {
    const fieldValue = value.colour;
    if (!checkColourConfiguration(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSkillRecord(input: unknown): boolean { return checkSkillRecord(input, {active: new Set<object>(), depth: 0}); }
function checkSkillRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.skillId === undefined) return false;
  if (value.skillId !== undefined) {
    const fieldValue = value.skillId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.instructions === undefined) return false;
  if (value.instructions !== undefined) {
    const fieldValue = value.instructions;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.resources === undefined) return false;
  if (value.resources !== undefined) {
    const fieldValue = value.resources;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkResourceVersionRef(item, context)) return false;
    }
  }
  if (value.capabilities === undefined) return false;
  if (value.capabilities !== undefined) {
    const fieldValue = value.capabilities;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSlateMetadata(input: unknown): boolean { return checkSlateMetadata(input, {active: new Set<object>(), depth: 0}); }
function checkSlateMetadata(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.projectId === undefined) return false;
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.sequences === undefined) return false;
  if (value.sequences !== undefined) {
    const fieldValue = value.sequences;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkSequenceView(item, context)) return false;
    }
  }
  if (value.media === undefined) return false;
  if (value.media !== undefined) {
    const fieldValue = value.media;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMediaView(item, context)) return false;
    }
  }
  if (value.timelines === undefined) return false;
  if (value.timelines !== undefined) {
    const fieldValue = value.timelines;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTimelineView(item, context)) return false;
    }
  }
  if (value.bins === undefined) return false;
  if (value.bins !== undefined) {
    const fieldValue = value.bins;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMediaBin(item, context)) return false;
    }
  }
  if (value.presets === undefined) return false;
  if (value.presets !== undefined) {
    const fieldValue = value.presets;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkRenderPreset(item, context)) return false;
    }
  }
  if (value.transcripts === undefined) return false;
  if (value.transcripts !== undefined) {
    const fieldValue = value.transcripts;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkResourceVersionRef(item, context)) return false;
    }
  }
  if (value.smallAssets === undefined) return false;
  if (value.smallAssets !== undefined) {
    const fieldValue = value.smallAssets;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkResourceVersionRef(item, context)) return false;
    }
  }
  if (value.contentRev === undefined) return false;
  if (value.contentRev !== undefined) {
    const fieldValue = value.contentRev;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  if (value.profile === undefined) return false;
  if (value.profile !== undefined) {
    const fieldValue = value.profile;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "slate.project.v1") return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSlateSelection(input: unknown): boolean { return checkSlateSelection(input, {active: new Set<object>(), depth: 0}); }
function checkSlateSelection(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.projectId === undefined) return false;
  if (value.projectId !== undefined) {
    const fieldValue = value.projectId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.sequenceId === undefined) return false;
  if (value.sequenceId !== undefined) {
    const fieldValue = value.sequenceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.range === undefined) return false;
  if (value.range !== undefined) {
    const fieldValue = value.range;
    if (!checkMediaRange(fieldValue, context)) return false;
  }
  if (value.items === undefined) return false;
  if (value.items !== undefined) {
    const fieldValue = value.items;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 10000) return false;
    for (const item of fieldValue) {
    if (!checkId(item, context)) return false;
    }
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkNativeContentRev(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isSourceConsentRef(input: unknown): boolean { return checkSourceConsentRef(input, {active: new Set<object>(), depth: 0}); }
function checkSourceConsentRef(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.consentId === undefined) return false;
  if (value.consentId !== undefined) {
    const fieldValue = value.consentId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.scopeHash === undefined) return false;
  if (value.scopeHash !== undefined) {
    const fieldValue = value.scopeHash;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[0-9a-f]{64}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.expiresAt === undefined) return false;
  if (value.expiresAt !== undefined) {
    const fieldValue = value.expiresAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isStructuredValue(input: unknown): boolean { return checkStructuredValue(input, {active: new Set<object>(), depth: 0}); }
function checkStructuredValue(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.value as {case?: string} | undefined)?.case === "null") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "boolean") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "integer") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "bigint") return false;
    if (fieldValue < -9223372036854775808n || fieldValue > 9223372036854775807n) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "decimal") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "text") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "instant") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "resource") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkResourceRef(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "list") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkValueList(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "record") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (!checkValueRecord(fieldValue, context)) return false;
  }
  if ((value.value as {case?: string} | undefined)?.case === "number") {
    const fieldValue = (value.value as {value: unknown}).value;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.value === undefined || typeof value.value !== 'object' || value.value === null) return false;
  if ((value.value as {case?: string}).case !== "null" && (value.value as {case?: string}).case !== "boolean" && (value.value as {case?: string}).case !== "integer" && (value.value as {case?: string}).case !== "decimal" && (value.value as {case?: string}).case !== "text" && (value.value as {case?: string}).case !== "instant" && (value.value as {case?: string}).case !== "resource" && (value.value as {case?: string}).case !== "list" && (value.value as {case?: string}).case !== "record" && (value.value as {case?: string}).case !== "number") return false;
  if (!structuredBounds(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTableBlock(input: unknown): boolean { return checkTableBlock(input, {active: new Set<object>(), depth: 0}); }
function checkTableBlock(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.rows === undefined) return false;
  if (value.rows !== undefined) {
    const fieldValue = value.rows;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 200) return false;
    for (const item of fieldValue) {
    if (!checkTableRow(item, context)) return false;
    }
  }
  if (!tableSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTableCell(input: unknown): boolean { return checkTableCell(input, {active: new Set<object>(), depth: 0}); }
function checkTableCell(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.cellId === undefined) return false;
  if (value.cellId !== undefined) {
    const fieldValue = value.cellId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.content === undefined) return false;
  if (value.content !== undefined) {
    const fieldValue = value.content;
    if (!checkRichText(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTableRow(input: unknown): boolean { return checkTableRow(input, {active: new Set<object>(), depth: 0}); }
function checkTableRow(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.rowId === undefined) return false;
  if (value.rowId !== undefined) {
    const fieldValue = value.rowId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.cells === undefined) return false;
  if (value.cells !== undefined) {
    const fieldValue = value.cells;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 50) return false;
    for (const item of fieldValue) {
    if (!checkTableCell(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTagRecord(input: unknown): boolean { return checkTagRecord(input, {active: new Set<object>(), depth: 0}); }
function checkTagRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.tagId === undefined) return false;
  if (value.tagId !== undefined) {
    const fieldValue = value.tagId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.notebookId === undefined) return false;
  if (value.notebookId !== undefined) {
    const fieldValue = value.notebookId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.colour !== undefined) {
    const fieldValue = value.colour;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.trashedAt !== undefined) {
    const fieldValue = value.trashedAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTaskSnapshot(input: unknown): boolean { return checkTaskSnapshot(input, {active: new Set<object>(), depth: 0}); }
function checkTaskSnapshot(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.taskId === undefined) return false;
  if (value.taskId !== undefined) {
    const fieldValue = value.taskId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.state === undefined) return false;
  if (value.state !== undefined) {
    const fieldValue = value.state;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4 && fieldValue !== 5 && fieldValue !== 6 && fieldValue !== 7 && fieldValue !== 8 && fieldValue !== 9) return false;
  }
  if (value.reason !== undefined) {
    const fieldValue = value.reason;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  if (value.revision === undefined) return false;
  if (value.revision !== undefined) {
    const fieldValue = value.revision;
    if (!checkRevision(fieldValue, context)) return false;
  }
  if (value.runId !== undefined) {
    const fieldValue = value.runId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.currentStreamId !== undefined) {
    const fieldValue = value.currentStreamId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.finalMessage !== undefined) {
    const fieldValue = value.finalMessage;
    if (!checkVersionedRef(fieldValue, context)) return false;
  }
  if (value.recoveryAt !== undefined) {
    const fieldValue = value.recoveryAt;
    if (!checkInstant(fieldValue, context)) return false;
  }
  if (value.control !== undefined) {
    const fieldValue = value.control;
    if (!checkControlProgress(fieldValue, context)) return false;
  }
  if (value.hasUnknownEffect === undefined) return false;
  if (value.hasUnknownEffect !== undefined) {
    const fieldValue = value.hasUnknownEffect;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.completedSteps === undefined) return false;
  if (value.completedSteps !== undefined) {
    const fieldValue = value.completedSteps;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.totalSteps === undefined) return false;
  if (value.totalSteps !== undefined) {
    const fieldValue = value.totalSteps;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.missingOutputs === undefined) return false;
  if (value.missingOutputs !== undefined) {
    const fieldValue = value.missingOutputs;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  if (value.reasonFacet === undefined) return false;
  if (value.reasonFacet !== undefined) {
    const fieldValue = value.reasonFacet;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3 && fieldValue !== 4 && fieldValue !== 5 && fieldValue !== 6) return false;
  }
  if ((value.state === 3) !== (value.reasonFacet !== 1) || (value.completedSteps as number) > (value.totalSteps as number)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTextRunSegment(input: unknown): boolean { return checkTextRunSegment(input, {active: new Set<object>(), depth: 0}); }
function checkTextRunSegment(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.runId === undefined) return false;
  if (value.runId !== undefined) {
    const fieldValue = value.runId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.from === undefined) return false;
  if (value.from !== undefined) {
    const fieldValue = value.from;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.until === undefined) return false;
  if (value.until !== undefined) {
    const fieldValue = value.until;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTextSpan(input: unknown): boolean { return checkTextSpan(input, {active: new Set<object>(), depth: 0}); }
function checkTextSpan(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.from === undefined) return false;
  if (value.from !== undefined) {
    const fieldValue = value.from;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.until === undefined) return false;
  if (value.until !== undefined) {
    const fieldValue = value.until;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.marks === undefined) return false;
  if (value.marks !== undefined) {
    const fieldValue = value.marks;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (typeof item !== "string") return false;
    if (!validUnicode(item)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(item)?.[0] !== item) return false;
    }
  }
  if (value.link !== undefined) {
    const fieldValue = value.link;
    if (!checkLinkSpec(fieldValue, context)) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTimedText(input: unknown): boolean { return checkTimedText(input, {active: new Set<object>(), depth: 0}); }
function checkTimedText(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.text === undefined) return false;
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if (utf8Length(fieldValue) > 262144) return false;
  }
  if (value.language !== undefined) {
    const fieldValue = value.language;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.fontFamily === undefined) return false;
  if (value.fontFamily !== undefined) {
    const fieldValue = value.fontFamily;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.fontAsset !== undefined) {
    const fieldValue = value.fontAsset;
    if (!checkResourceVersionRef(fieldValue, context)) return false;
  }
  if (value.size === undefined) return false;
  if (value.size !== undefined) {
    const fieldValue = value.size;
    if (!checkDecimal(fieldValue, context)) return false;
  }
  if (value.colour === undefined) return false;
  if (value.colour !== undefined) {
    const fieldValue = value.colour;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.alignment === undefined) return false;
  if (value.alignment !== undefined) {
    const fieldValue = value.alignment;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTimelineClip(input: unknown): boolean { return checkTimelineClip(input, {active: new Set<object>(), depth: 0}); }
function checkTimelineClip(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.clipId === undefined) return false;
  if (value.clipId !== undefined) {
    const fieldValue = value.clipId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.mediaId !== undefined) {
    const fieldValue = value.mediaId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.timeline === undefined) return false;
  if (value.timeline !== undefined) {
    const fieldValue = value.timeline;
    if (!checkMediaRange(fieldValue, context)) return false;
  }
  if (value.source !== undefined) {
    const fieldValue = value.source;
    if (!checkMediaRange(fieldValue, context)) return false;
  }
  if (value.playbackRate === undefined) return false;
  if (value.playbackRate !== undefined) {
    const fieldValue = value.playbackRate;
    if (!checkRational(fieldValue, context)) return false;
  }
  if (value.effects === undefined) return false;
  if (value.effects !== undefined) {
    const fieldValue = value.effects;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkEffectSpec(item, context)) return false;
    }
  }
  if (value.origin !== undefined) {
    const fieldValue = value.origin;
    if (!checkContentOrigin(fieldValue, context)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "clip" && fieldValue !== "gap" && fieldValue !== "title" && fieldValue !== "subtitleCue" && fieldValue !== "generatedMedia" && fieldValue !== "nestedSequence" && fieldValue !== "adjustment") return false;
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.nestedSequenceId !== undefined) {
    const fieldValue = value.nestedSequenceId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.generated !== undefined) {
    const fieldValue = value.generated;
    if (!checkGeneratedSource(fieldValue, context)) return false;
  }
  if (value.text !== undefined) {
    const fieldValue = value.text;
    if (!checkTimedText(fieldValue, context)) return false;
  }
  if (value.graph === undefined) return false;
  if (value.graph !== undefined) {
    const fieldValue = value.graph;
    if (!checkProcessingGraph(fieldValue, context)) return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.linkGroupId !== undefined) {
    const fieldValue = value.linkGroupId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.retime !== undefined) {
    const fieldValue = value.retime;
    if (!checkRetimeCurve(fieldValue, context)) return false;
  }
  if (value.groupId !== undefined) {
    const fieldValue = value.groupId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTimelineTrack(input: unknown): boolean { return checkTimelineTrack(input, {active: new Set<object>(), depth: 0}); }
function checkTimelineTrack(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.trackId === undefined) return false;
  if (value.trackId !== undefined) {
    const fieldValue = value.trackId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "video" && fieldValue !== "audio" && fieldValue !== "subtitle") return false;
  }
  if (value.order === undefined) return false;
  if (value.order !== undefined) {
    const fieldValue = value.order;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
  }
  if (value.clips === undefined) return false;
  if (value.clips !== undefined) {
    const fieldValue = value.clips;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTimelineClip(item, context)) return false;
    }
  }
  if (value.transitions === undefined) return false;
  if (value.transitions !== undefined) {
    const fieldValue = value.transitions;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTransitionSpec(item, context)) return false;
    }
  }
  if (value.enabled === undefined) return false;
  if (value.enabled !== undefined) {
    const fieldValue = value.enabled;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ([...fieldValue].length > 256) return false;
  }
  if (value.locked === undefined) return false;
  if (value.locked !== undefined) {
    const fieldValue = value.locked;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.graph === undefined) return false;
  if (value.graph !== undefined) {
    const fieldValue = value.graph;
    if (!checkProcessingGraph(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTimelineView(input: unknown): boolean { return checkTimelineView(input, {active: new Set<object>(), depth: 0}); }
function checkTimelineView(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.sequence === undefined) return false;
  if (value.sequence !== undefined) {
    const fieldValue = value.sequence;
    if (!checkSequenceView(fieldValue, context)) return false;
  }
  if (value.tracks === undefined) return false;
  if (value.tracks !== undefined) {
    const fieldValue = value.tracks;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkTimelineTrack(item, context)) return false;
    }
  }
  if (value.markers === undefined) return false;
  if (value.markers !== undefined) {
    const fieldValue = value.markers;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkMarkerView(item, context)) return false;
    }
  }
  if (value.graph === undefined) return false;
  if (value.graph !== undefined) {
    const fieldValue = value.graph;
    if (!checkProcessingGraph(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isToolProposal(input: unknown): boolean { return checkToolProposal(input, {active: new Set<object>(), depth: 0}); }
function checkToolProposal(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.proposalId === undefined) return false;
  if (value.proposalId !== undefined) {
    const fieldValue = value.proposalId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.capability === undefined) return false;
  if (value.capability !== undefined) {
    const fieldValue = value.capability;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.arguments === undefined) return false;
  if (value.arguments !== undefined) {
    const fieldValue = value.arguments;
    if (!checkCapabilityArguments(fieldValue, context)) return false;
  }
  if (value.sourceAttemptId === undefined) return false;
  if (value.sourceAttemptId !== undefined) {
    const fieldValue = value.sourceAttemptId;
    if (!checkId(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isToolResult(input: unknown): boolean { return checkToolResult(input, {active: new Set<object>(), depth: 0}); }
function checkToolResult(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.invocationId === undefined) return false;
  if (value.invocationId !== undefined) {
    const fieldValue = value.invocationId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.effect === undefined) return false;
  if (value.effect !== undefined) {
    const fieldValue = value.effect;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < -2147483648 || fieldValue > 2147483647) return false;
    if (fieldValue !== 1 && fieldValue !== 2 && fieldValue !== 3) return false;
  }
  if ((value.outcome as {case?: string} | undefined)?.case === "success") {
    const fieldValue = (value.outcome as {value: unknown}).value;
    if (!checkCapabilityResult(fieldValue, context)) return false;
  }
  if ((value.outcome as {case?: string} | undefined)?.case === "error") {
    const fieldValue = (value.outcome as {value: unknown}).value;
    if (!checkArcError(fieldValue, context)) return false;
  }
  if (value.artifacts === undefined) return false;
  if (value.artifacts !== undefined) {
    const fieldValue = value.artifacts;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkArtifactRef(item, context)) return false;
    }
  }
  if (value.outcome === undefined || typeof value.outcome !== 'object' || value.outcome === null) return false;
  if ((value.outcome as {case?: string}).case !== "success" && (value.outcome as {case?: string}).case !== "error") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTransitionSpec(input: unknown): boolean { return checkTransitionSpec(input, {active: new Set<object>(), depth: 0}); }
function checkTransitionSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.transitionId === undefined) return false;
  if (value.transitionId !== undefined) {
    const fieldValue = value.transitionId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.leftClipId === undefined) return false;
  if (value.leftClipId !== undefined) {
    const fieldValue = value.leftClipId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.rightClipId === undefined) return false;
  if (value.rightClipId !== undefined) {
    const fieldValue = value.rightClipId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "dissolve" && fieldValue !== "audioCrossfade") return false;
  }
  if (value.inOffset === undefined) return false;
  if (value.inOffset !== undefined) {
    const fieldValue = value.inOffset;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  if (value.outOffset === undefined) return false;
  if (value.outOffset !== undefined) {
    const fieldValue = value.outOffset;
    if (!checkMediaTime(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTriggerConfiguration(input: unknown): boolean { return checkTriggerConfiguration(input, {active: new Set<object>(), depth: 0}); }
function checkTriggerConfiguration(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.kind === undefined) return false;
  if (value.kind !== undefined) {
    const fieldValue = value.kind;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "manual" && fieldValue !== "edge") return false;
  }
  if (value.channelId !== undefined) {
    const fieldValue = value.channelId;
    if (!checkId(fieldValue, context)) return false;
  }
  if (value.threshold !== undefined) {
    const fieldValue = value.threshold;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.direction !== undefined) {
    const fieldValue = value.direction;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
    if (fieldValue !== "rising" && fieldValue !== "falling" && fieldValue !== "either") return false;
  }
  if (value.hysteresis === undefined) return false;
  if (value.hysteresis !== undefined) {
    const fieldValue = value.hysteresis;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isFinite(fieldValue)) return false;
  }
  if (value.holdoff === undefined) return false;
  if (value.holdoff !== undefined) {
    const fieldValue = value.holdoff;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.pre === undefined) return false;
  if (value.pre !== undefined) {
    const fieldValue = value.pre;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.post === undefined) return false;
  if (value.post !== undefined) {
    const fieldValue = value.post;
    if (!checkScopeTime(fieldValue, context)) return false;
  }
  if (value.repeated === undefined) return false;
  if (value.repeated !== undefined) {
    const fieldValue = value.repeated;
    if (typeof fieldValue !== "boolean") return false;
  }
  if (value.maxOccurrences === undefined) return false;
  if (value.maxOccurrences !== undefined) {
    const fieldValue = value.maxOccurrences;
    if (typeof fieldValue !== "number") return false;
    if (!Number.isInteger(fieldValue) || fieldValue < 0 || fieldValue > 4294967295) return false;
    if (fieldValue < 1) return false;
  }
  if (!triggerSemantics(value)) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTriggerSpec(input: unknown): boolean { return checkTriggerSpec(input, {active: new Set<object>(), depth: 0}); }
function checkTriggerSpec(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if ((value.trigger as {case?: string} | undefined)?.case === "manual") {
    const fieldValue = (value.trigger as {value: unknown}).value;
    if (typeof fieldValue !== "boolean") return false;
  }
  if ((value.trigger as {case?: string} | undefined)?.case === "schedule") {
    const fieldValue = (value.trigger as {value: unknown}).value;
    if (!checkScheduleSpec(fieldValue, context)) return false;
  }
  if ((value.trigger as {case?: string} | undefined)?.case === "event") {
    const fieldValue = (value.trigger as {value: unknown}).value;
    if (!checkEventTrigger(fieldValue, context)) return false;
  }
  if (value.trigger === undefined || typeof value.trigger !== 'object' || value.trigger === null) return false;
  if ((value.trigger as {case?: string}).case !== "manual" && (value.trigger as {case?: string}).case !== "schedule" && (value.trigger as {case?: string}).case !== "event") return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isTurnInput(input: unknown): boolean { return checkTurnInput(input, {active: new Set<object>(), depth: 0}); }
function checkTurnInput(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.message !== undefined) {
    const fieldValue = value.message;
    if (!checkMessageDraft(fieldValue, context)) return false;
  }
  if (value.context === undefined) return false;
  if (value.context !== undefined) {
    const fieldValue = value.context;
    if (!Array.isArray(fieldValue)) return false;
    for (const item of fieldValue) {
    if (!checkContextRef(item, context)) return false;
    }
  }
  if (value.instructions !== undefined) {
    const fieldValue = value.instructions;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isValueEntry(input: unknown): boolean { return checkValueEntry(input, {active: new Set<object>(), depth: 0}); }
function checkValueEntry(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.name === undefined) return false;
  if (value.name !== undefined) {
    const fieldValue = value.name;
    if (typeof fieldValue !== "string") return false;
    if (!validUnicode(fieldValue)) return false;
    if ((new RegExp("^[A-Za-z0-9._:/-]{1,128}$", 'u')).exec(fieldValue)?.[0] !== fieldValue) return false;
  }
  if (value.value === undefined) return false;
  if (value.value !== undefined) {
    const fieldValue = value.value;
    if (!checkStructuredValue(fieldValue, context)) return false;
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isValueList(input: unknown): boolean { return checkValueList(input, {active: new Set<object>(), depth: 0}); }
function checkValueList(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.items === undefined) return false;
  if (value.items !== undefined) {
    const fieldValue = value.items;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 200) return false;
    for (const item of fieldValue) {
    if (!checkStructuredValue(item, context)) return false;
    }
  }
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
export function isValueRecord(input: unknown): boolean { return checkValueRecord(input, {active: new Set<object>(), depth: 0}); }
function checkValueRecord(input: unknown, context: ValidationContext): boolean {
  if (typeof input !== 'object' || input === null || Array.isArray(input) || context.depth >= 100 || context.active.has(input)) return false;
  context.active.add(input); context.depth++;
  try {
  const value = input as Record<string, unknown>;
  if (value.entries === undefined) return false;
  if (value.entries !== undefined) {
    const fieldValue = value.entries;
    if (!Array.isArray(fieldValue)) return false;
    if (fieldValue.length > 200) return false;
    for (const item of fieldValue) {
    if (!checkValueEntry(item, context)) return false;
    }
  }
  if (!orderedKeys(((value.entries ?? []) as Profile[]).map(v => v.name as string))) return false;
  return true;
  } finally { context.depth--; context.active.delete(input); }
}
type ValidationContext = {active: Set<object>; depth: number};
function validUnicode(text: string): boolean {
  for (let i = 0; i < text.length; i++) {
    const unit = text.charCodeAt(i);
    if (unit < 0xd800 || unit > 0xdfff) continue;
    if (unit > 0xdbff || ++i >= text.length) return false;
    const low = text.charCodeAt(i);
    if (low < 0xdc00 || low > 0xdfff) return false;
  }
  return true;
}

function canonicalDecimal(value: string): boolean {
  if (/^-?(0|[1-9][0-9]*)(\.[0-9]{1,9})?$/.exec(value)?.[0] !== value) return false;
  if (value.startsWith('-') && !/[1-9]/.test(value)) return false;
  return value.replace(/[-.]/g, '').replace(/^0+/, '').length <= 28;
}
function utf8Length(value: string): number {
  let size = 0;
  for (const scalar of value) {
    const point = scalar.codePointAt(0)!;
    size += point <= 0x7f ? 1 : point <= 0x7ff ? 2 : point <= 0xffff ? 3 : 4;
  }
  return size;
}

// These helpers follow successful recursive field checks. They never deserialize
// a second wire model or consult an owner store.
type Profile = Record<string, any>;
function reduced(numerator: bigint, denominator: bigint): boolean {
  if (denominator === 0n) return false;
  let a = numerator < 0n ? -numerator : numerator;
  let b = denominator;
  while (b !== 0n) { const r = a % b; a = b; b = r; }
  return a === 1n;
}
function safeLink(value: string): boolean { return /^(?:https?:\/\/[^\s/?#]+(?:[^\s]*)|mailto:[^\s]+)$/.exec(value)?.[0] === value; }
function idKey(value: Profile): string { return Array.from(value.value as Uint8Array, x => x.toString(16).padStart(2, '0')).join(''); }
function uniqueIds(values: Profile[]): boolean { return new Set(values.map(idKey)).size === values.length; }
function orderedKeys(values: string[]): boolean { return values.every((v, i) => i === 0 || values[i - 1]! < v); }
function orderedIds(values: Profile[]): boolean { return orderedKeys(values.map(idKey)); }
function compareInstant(a: Profile, b: Profile): number { return a.unixSeconds < b.unixSeconds ? -1 : a.unixSeconds > b.unixSeconds ? 1 : Math.sign(a.nanos - b.nanos); }
function mediaRangeSemantics(value: Profile): boolean {
  return value.duration.ticks >= 0n && value.start.rate.numerator === value.duration.rate.numerator && value.start.rate.denominator === value.duration.rate.denominator && value.start.ticks + value.duration.ticks <= 9223372036854775807n;
}
function originSemantics(value: Profile): boolean {
  const kinds = ['aiGenerated','aiManipulated','nonAi','unknown'];
  const positions = (value.kinds ?? []).map((k: string) => kinds.indexOf(k)) as number[];
  const parents = (value.parentOriginIds ?? []) as Profile[];
  return positions.length > 0 && positions.every((v, i) => v >= 0 && (i === 0 || positions[i - 1]! < v)) && orderedIds(parents) && (value.omittedParentCount === 0 || parents.length === 32) && parents.every(v => idKey(v) !== idKey(value.originId));
}
function resourceVersionSemantics(value: Profile): boolean {
  return !(value.revision.case === 'cloud' && value.revision.value.value <= 0n) && (value.blob === undefined || (value.contentHash !== undefined && value.contentHash === value.blob.contentHash));
}
const errorCategories: Record<string, number> = {
  'validation.invalid_request':1,'validation.ast_bounds_exceeded':1,'validation.unsupported_version':1,'validation.invalid_offset':1,'media.time_not_representable':1,
  'auth.unauthenticated':2,'auth.session_expired':2,'auth.step_up_required':2,
  'auth.local_presence_required':3,'perm.capability_denied':3,'perm.resource_denied':3,'perm.egress_denied':3,'perm.approval_required':3,'perm.approval_expired':3,'perm.lease_expired':3,
  'entitlement.no_service_term':4,'entitlement.not_entitled':4,'entitlement.quota_exceeded':4,'entitlement.capacity_exhausted':4,'entitlement.extra_credits_required':4,'entitlement.credits_exhausted':4,'entitlement.request_too_large':4,'commerce.supplier_budget_exhausted':4,
  'conflict.revision_mismatch':5,'conflict.local_changes_pending':5,'conflict.duplicate_identifier':5,'command.reused_identifier':5,
  'state.not_found':6,'state.invalid_transition':6,'state.gone':6,'state.stale_fence':6,'sync.cursor_expired':6,'sync.bootstrap_expired':6,'identity.last_credential':6,
  'resource.unavailable':7,'resource.integrity_failed':7,'resource.upload_expired':7,'resource.parser_failed':7,'capacity.rate_limited':7,'capacity.busy':7,
  'dependency.unavailable':8,'dependency.timeout':8,'provider.declined':8,'security.isolation_unavailable':8,'internal.unexpected':9,
};
const noRetryCodes = new Set(['auth.unauthenticated','auth.session_expired','auth.step_up_required','auth.local_presence_required','perm.capability_denied','perm.resource_denied','perm.egress_denied','perm.approval_required','perm.approval_expired','perm.lease_expired','entitlement.no_service_term','entitlement.not_entitled','entitlement.extra_credits_required','entitlement.credits_exhausted','validation.invalid_request','validation.ast_bounds_exceeded','validation.unsupported_version','identity.last_credential','conflict.duplicate_identifier','command.reused_identifier','state.not_found','state.invalid_transition','state.gone','resource.integrity_failed']);
function errorSemantics(value: Profile): boolean {
  const category = Object.hasOwn(errorCategories, value.code as string) ? errorCategories[value.code as string] : undefined;
  if (category !== undefined && value.category !== category) return false;
  if (category !== undefined && !['dependency.unavailable','dependency.timeout','internal.unexpected','resource.parser_failed'].includes(value.code) && value.effect !== 1) return false;
  if (value.code === 'dependency.timeout' && value.effect !== 3) return false;
  if (['entitlement.capacity_exhausted','capacity.rate_limited','capacity.busy'].includes(value.code) && value.retry.mode !== 3) return false;
  if (noRetryCodes.has(value.code) && value.retry.mode !== 1) return false;
  return !(value.effect === 3 && value.retry.mode === 3);
}
function validDate(value: string): boolean {
  if (/^\d{4}-\d{2}-\d{2}$/.exec(value)?.[0] !== value) return false;
  const year = Number(value.slice(0,4)), month = Number(value.slice(5,7)), day = Number(value.slice(8,10));
  if (year < 1 || month < 1 || month > 12) return false;
  const leap = year % 4 === 0 && (year % 100 !== 0 || year % 400 === 0);
  return day >= 1 && day <= [31,leap ? 29 : 28,31,30,31,30,31,31,30,31,30,31][month - 1]!;
}
function scalarSemantics(value: Profile): boolean {
  const scalar = value.value as Profile;
  switch (scalar.case) {
    case 'null': return scalar.value === true;
    case 'number': return !scalar.value.value.includes('.') || !scalar.value.value.endsWith('0');
    case 'date': return validDate(scalar.value);
    case 'multiSelect': return (scalar.value.items ?? []).length <= 100 && orderedIds(scalar.value.items ?? []);
    case 'dateTime': return scalar.value.nanos % 100 === 0;
    default: return true;
  }
}
function predicateSemantics(value: Profile): boolean {
  const operands = (value.operands ?? []) as Profile[];
  if (['isMissing','isPresent'].includes(value.operator)) return operands.length === 0;
  const membership = ['in','hasAny','hasAll'].includes(value.operator);
  if (membership ? operands.length < 1 || operands.length > 100 : operands.length !== 1) return false;
  const kind = operands[0]!.value.case;
  if (kind === 'null' || operands.some(v => v.value.case !== kind)) return false;
  if (operands.some(v => ['text','url'].includes(v.value.case) && [...v.value.value as string].length > 4096)) return false;
  if (['hasAny','hasAll'].includes(value.operator)) return kind === 'select';
  if (['contains','startsWith','endsWith'].includes(value.operator)) return ['text','url'].includes(kind);
  if (['eq','ne'].includes(value.operator)) return true;
  return kind !== 'multiSelect' && ['lt','le','gt','ge','in'].includes(value.operator);
}
function filterBounds(root: Profile): boolean {
  const pending: [Profile, number][] = [[root, 1]];
  let count = 0;
  while (pending.length) {
    const [node, depth] = pending.pop()!;
    if (++count > 128 || depth > 8) return false;
    if (node.expression.case === 'group') for (const child of node.expression.value.children ?? []) pending.push([child, depth + 1]);
  }
  return true;
}
function querySemantics(value: Profile): boolean {
  if ((value.savedViewId === undefined) !== (value.savedViewRev === undefined) || (value.savedViewRev !== undefined && value.savedViewRev.value <= 0n)) return false;
  const projection = (value.projection ?? []) as Profile[], sorts = (value.sorts ?? []) as Profile[], versions = (value.definitionVersions ?? []) as Profile[];
  if (!uniqueIds(projection) || !uniqueIds(sorts.map(v => v.propertyId)) || !uniqueIds(versions.map(v => v.propertyId))) return false;
  const definitions = new Set(versions.map(v => idKey(v.propertyId)));
  if (versions.some(v => v.semanticRevision.value <= 0n) || projection.some(v => !definitions.has(idKey(v))) || sorts.some(v => !definitions.has(idKey(v.propertyId)))) return false;
  const pending = value.filter === undefined ? [] : [value.filter as Profile];
  let count = 0;
  while (pending.length) {
    const node = pending.pop()!;
    if (++count > 128) return false;
    if (node.expression.case === 'predicate' && !definitions.has(idKey(node.expression.value.propertyId))) return false;
    if (node.expression.case === 'group') pending.push(...node.expression.value.children ?? []);
  }
  return true;
}
function definitionSemantics(value: Profile): boolean {
  const options = (value.options ?? []) as Profile[];
  return value.semanticRevision.value > 0n && value.revision.value > 0n && (value.numberScale === undefined || value.type === 'number') && (['select','multiSelect'].includes(value.type) || options.length === 0) && uniqueIds(options.map(v => v.optionId)) && new Set(options.map(v => v.order)).size === options.length;
}
function savedViewSemantics(value: Profile): boolean {
  return value.query.page.cursor === undefined && value.query.datasetToken === undefined && value.query.savedViewId === undefined && value.query.savedViewRev === undefined && idKey(value.notebookId) === idKey(value.query.notebookId);
}
function structuredBounds(root: Profile): boolean {
  const pending: [Profile, number][] = [[root,1]];
  while (pending.length) {
    const [node, depth] = pending.pop()!;
    if (depth > 16) return false;
    if (node.value.case === 'null' && node.value.value !== true) return false;
    if (node.value.case === 'list') for (const child of node.value.value.items ?? []) pending.push([child, depth + 1]);
    if (node.value.case === 'record') for (const entry of node.value.value.entries ?? []) pending.push([entry.value, depth + 1]);
  }
  return true;
}
function tableSemantics(value: Profile): boolean {
  const rows = (value.rows ?? []) as Profile[];
  if (!rows.length) return false;
  const width = (rows[0]!.cells ?? []).length;
  return width >= 1 && width <= 50 && rows.every(v => (v.cells ?? []).length === width) && uniqueIds(rows.map(v => v.rowId)) && uniqueIds(rows.flatMap(v => (v.cells ?? []).map((c: Profile) => c.cellId)));
}
function boundary(text: string, offset: number): boolean { return offset <= text.length && (offset === 0 || offset === text.length || text.charCodeAt(offset) < 0xdc00 || text.charCodeAt(offset) > 0xdfff); }
function richTextSemantics(value: Profile): boolean {
  const text = value.text as string, runs = (value.runs ?? []) as Profile[], atoms = (value.atoms ?? []) as Profile[], spans = (value.spans ?? []) as Profile[];
  if (text.normalize('NFC') !== text) return false;
  if (!text.length) return runs.length === 1 && runs[0]!.from === 0 && runs[0]!.until === 0 && atoms.length === 0 && spans.length === 0;
  if (!uniqueIds([...runs.map(v => v.runId), ...atoms.map(v => v.inlineId)])) return false;
  const intervals: [number, number][] = [];
  for (const run of runs) {
    if (run.from >= run.until || !boundary(text, run.from) || !boundary(text, run.until) || text.slice(run.from, run.until).includes('\uFFFC')) return false;
    intervals.push([run.from, run.until]);
  }
  for (const atom of atoms) {
    if (atom.offset >= text.length || text[atom.offset] !== '\uFFFC' || (atom.content.case === 'math' && atom.content.value.display)) return false;
    intervals.push([atom.offset, atom.offset + 1]);
  }
  intervals.sort((a,b) => a[0] - b[0]);
  let end = 0;
  for (const interval of intervals) { if (interval[0] !== end) return false; end = interval[1]; }
  if (end !== text.length) return false;
  let spanEnd = 0;
  for (const span of spans) {
    if (span.from >= span.until || span.from < spanEnd || !boundary(text, span.from) || !boundary(text, span.until) || new Set(span.marks ?? []).size !== (span.marks ?? []).length) return false;
    spanEnd = span.until;
  }
  return true;
}
function blockSemantics(value: Profile): boolean {
  const expected: Record<string, string> = {paragraph:'text',heading:'text',list:'text',quote:'text',callout:'text',toggle:'text',code:'code',image:'resource',attachment:'resource',embed:'link',table:'table',math:'math',divider:'empty'};
  const body = value.body.content;
  if (expected[value.kind] === undefined || body.case !== expected[value.kind] || (body.case === 'empty' && body.value !== true) || (body.case === 'math' && body.value.display !== true)) return false;
  const p = value.properties as Profile | undefined;
  if (p === undefined) return !['heading','list','callout','image','attachment','embed'].includes(value.kind);
  if ((p.headingLevel !== undefined) !== (value.kind === 'heading') || (p.listStyle !== undefined) !== (value.kind === 'list') || (p.calloutKind !== undefined) !== (value.kind === 'callout') || (p.altText !== undefined) !== (value.kind === 'image') || (p.imageLayout !== undefined) !== (value.kind === 'image') || (p.attachmentPresentation !== undefined) !== (value.kind === 'attachment') || (p.embedRenderMode !== undefined) !== (value.kind === 'embed')) return false;
  return (p.checked !== undefined) === (value.kind === 'list' && p.listStyle === 'checklist');
}
function documentSemantics(value: Profile): boolean {
  const rows = (value.blocks ?? []) as Profile[], properties = (value.properties ?? []) as Profile[];
  if (!uniqueIds(rows.map(v => v.blockId)) || !uniqueIds(properties.map(v => v.propertyId)) || !uniqueIds(value.tags ?? [])) return false;
  const blocks = new Map(rows.map(v => [idKey(v.blockId), v])), positions = new Set<string>();
  for (const block of rows) {
    const position = (block.parentId === undefined ? 'root' : idKey(block.parentId)) + ':' + block.orderKey;
    if (positions.has(position)) return false;
    positions.add(position);
    const seen = new Set([idKey(block.blockId)]);
    let parent = block.parentId as Profile | undefined;
    while (parent !== undefined) {
      const key = idKey(parent), parentBlock = blocks.get(key);
      if (seen.has(key) || parentBlock === undefined) return false;
      seen.add(key); parent = parentBlock.parentId;
    }
  }
  return true;
}
function compareScopeTime(a: Profile, b: Profile): number {
  const left = (a.ticks as bigint) * (a.rate.denominator as bigint) * (b.rate.numerator as bigint);
  const right = (b.ticks as bigint) * (b.rate.denominator as bigint) * (a.rate.numerator as bigint);
  return left < right ? -1 : left > right ? 1 : 0;
}
function alignmentSemantics(value: Profile): boolean {
  const left = value.leftAnchor !== undefined, right = value.rightAnchor !== undefined, offset = value.offset !== undefined, event = value.eventId !== undefined;
  switch (value.kind) {
    case 'absoluteTime': return !left && !right && !offset && !event;
    case 'trigger': return left && right && !offset && !event;
    case 'event': return left && right && !offset && event;
    case 'manualOffset': return !left && !right && offset && !event;
    default: return false;
  }
}
function cursorSemantics(value: Profile): boolean {
  const a = value.a.time, b = value.b.time, d = value.deltaTime;
  const left = (d.ticks as bigint) * (d.rate.denominator as bigint) * (a.rate.numerator as bigint) * (b.rate.numerator as bigint);
  const right = ((b.ticks as bigint) * (b.rate.denominator as bigint) * (a.rate.numerator as bigint) - (a.ticks as bigint) * (a.rate.denominator as bigint) * (b.rate.numerator as bigint)) * (d.rate.numerator as bigint);
  const expected = value.b.value - value.a.value;
  return left === right && Number.isFinite(expected) && Math.abs(value.deltaValue - expected) <= 1e-12 + 1e-9 * Math.abs(expected);
}
function configurationSemantics(value: Profile): boolean {
  const rows = (value.channels ?? []) as Profile[];
  if (!uniqueIds(rows.map(v => v.channelId))) return false;
  const channels = new Set(rows.map(v => idKey(v.channelId)));
  if ((value.framing.fields ?? []).some((v: Profile) => !channels.has(idKey(v.channelId)))) return false;
  return value.trigger?.channelId === undefined || channels.has(idKey(value.trigger.channelId));
}
function frameSemantics(value: Profile): boolean {
  const fields = (value.fields ?? []) as Profile[];
  if (!uniqueIds(fields.map(v => v.channelId))) return false;
  const start = value.start as Uint8Array, end = value.end as Uint8Array;
  const newline = end.length === 1 && end[0] === 10 || end.length === 2 && end[0] === 13 && end[1] === 10;
  switch (value.kind) {
    case 'delimitedText': return start.length <= 16 && end.length >= 1 && end.length <= 16 && value.delimiter === undefined && value.frameBytes === undefined && !value.header && fields.every(v => v.offset === undefined && (v.jsonPath ?? []).length === 0);
    case 'csvLine': return start.length === 0 && newline && value.escapeByte === undefined && [44,59,9].includes(value.delimiter) && value.frameBytes === undefined && fields.every(v => v.column !== undefined && v.offset === undefined && (v.jsonPath ?? []).length === 0);
    case 'jsonLine': return start.length === 0 && newline && value.escapeByte === undefined && value.delimiter === undefined && value.frameBytes === undefined && !value.header && fields.every(v => v.column === undefined && v.offset === undefined && (v.jsonPath ?? []).length !== 0);
    case 'canonicalReplay': return start.length === 0 && end.length === 0 && value.escapeByte === undefined && value.delimiter === undefined && value.frameBytes === undefined && !value.header && fields.length === 0 && value.checksum === undefined;
    case 'fixedBinary': {
      if (start.length || end.length || value.escapeByte !== undefined || value.delimiter !== undefined || value.header || value.frameBytes === undefined || value.frameBytes < 1 || value.frameBytes > 1048576 || !['little','big'].includes(value.byteOrder)) return false;
      const widths: Record<string, number> = {u8:1,i8:1,bool:1,u16:2,i16:2,u32:4,i32:4,f32:4,u64:8,i64:8,f64:8};
      const intervals: [number, number][] = [];
      for (const field of fields) {
        const width = widths[field.scalarType as string];
        if (width === undefined || field.offset === undefined || field.column !== undefined || (field.jsonPath ?? []).length || field.offset + width > value.frameBytes) return false;
        const end = field.offset + width;
        if (intervals.some(([from, until]) => field.offset < until && from < end)) return false;
        intervals.push([field.offset, end]);
      }
      if (value.checksum !== undefined) {
        const checksumWidths: Record<string, number> = {xor8:1,crc16CcittFalse:2,crc32IsoHdlc:4};
        const checksum = value.checksum, width = checksumWidths[checksum.algorithm as string];
        if (width === undefined || checksum.offset + width > value.frameBytes || checksum.input.offset + checksum.input.length > BigInt(value.frameBytes)) return false;
      }
      return true;
    }
    default: return false;
  }
}
function checksumSemantics(value: Profile): boolean {
  const widths: Record<string, bigint> = {xor8:1n,crc16CcittFalse:2n,crc32IsoHdlc:4n};
  const width = widths[value.algorithm as string];
  return width !== undefined && ['little','big'].includes(value.byteOrder) &&
    (value.input.offset + value.input.length <= BigInt(value.offset) || BigInt(value.offset) + width <= value.input.offset);
}
function triggerSemantics(value: Profile): boolean {
  if (value.hysteresis < 0 || value.holdoff.ticks < 0n || value.pre.ticks < 0n || value.post.ticks < 0n || value.maxOccurrences === 0 || (!value.repeated && value.maxOccurrences !== 1)) return false;
  return value.kind === 'manual' ? value.channelId === undefined && value.threshold === undefined && value.direction === undefined && value.hysteresis === 0 : value.kind === 'edge' && value.channelId !== undefined && value.threshold !== undefined && ['rising','falling','either'].includes(value.direction);
}
function pulseFamily(family: string): boolean { return ['frequency','dutyCycle','riseTime','fallTime'].includes(family); }
function thresholdSemantics(value: Profile): boolean {
  if (!pulseFamily(value.family)) return false;
  switch (value.name) {
    case 'low': case 'high': return true;
    case 'fraction10': return value.unit === '1' && value.value === 0.1;
    case 'fraction50': return value.unit === '1' && value.value === 0.5;
    case 'fraction90': return value.unit === '1' && value.value === 0.9;
    default: return false;
  }
}
function fixedUnit(name: string): string | undefined {
  if (['count','eventCount','dutyCycle'].includes(name)) return '1';
  if (['duration','riseTime','fallTime','deltaTime'].includes(name)) return 's';
  return name === 'frequency' ? 'Hz' : undefined;
}
function familySemantics(value: Profile): boolean {
  const values = (value.values ?? []) as Profile[];
  if (value.status !== 'ok') {
    if (values.length || value.reason === undefined) return false;
    if (value.status === 'invalid') return ['invalidTimeOrder','invalidConfiguration','numericOverflow'].includes(value.reason);
    if (value.status !== 'insufficient') return false;
    switch (value.reason) {
      case 'noFiniteSamples': return !['count','duration','eventCount'].includes(value.family);
      case 'noCompleteCycle': return ['frequency','dutyCycle'].includes(value.family);
      case 'noCompleteEdge': return ['riseTime','fallTime'].includes(value.family);
      case 'cursorUnavailable': return value.family === 'cursorDelta';
      default: return false;
    }
  }
  if (value.reason !== undefined) return false;
  if (value.validCount === 0n && ['minimum','maximum','mean','rms','peakToPeak','standardDeviation'].includes(value.family)) return false;
  if (value.family === 'cursorDelta') {
    if (values.length !== 2 || !values.some(v => v.name === 'deltaTime') || !values.some(v => v.name === 'deltaValue')) return false;
  } else if (values.length !== 1 || values[0]!.name !== value.family) return false;
  for (const item of values) {
    const unit = fixedUnit(item.name);
    if (unit !== undefined && item.unit !== unit) return false;
    if (item.name === 'dutyCycle' && (item.result.value < 0 || item.result.value > 1)) return false;
    if (['duration','frequency'].includes(item.name) && item.result.value <= 0) return false;
    if (['rms','peakToPeak','standardDeviation','riseTime','fallTime'].includes(item.name) && item.result.value < 0) return false;
    if (item.name === 'count' && item.result.value !== value.validCount) return false;
  }
  return true;
}
function sourceSemantics(value: Profile, configuration: Profile): boolean {
  if (value.capture.contentHash === undefined || value.configuration.contentHash === undefined) return false;
  if ([value.decoder,value.timeMapping,value.eventSet].some(v => v !== undefined && v.contentHash === undefined)) return false;
  if (value.configuration.revision.case === 'native' && value.configuration.revision.value.value !== configuration.revision.value) return false;
  return (configuration.channels ?? []).some((v: Profile) => idKey(v.channelId) === idKey(value.channelId));
}
function thresholdBindings(values: Profile[], channels: Map<string, Profile>, families: Set<string>, result: boolean, levels: boolean, fractions: boolean): Map<string, Profile> | undefined {
  const resolved = new Map<string, Profile>();
  for (const threshold of values) {
    if (!families.has(threshold.family) || (result && threshold.channelId === undefined)) return undefined;
    const level = ['low','high'].includes(threshold.name);
    if (level ? !levels : !fractions) return undefined;
    const targets = threshold.channelId === undefined ? [...channels].filter(([, channel]) => !level || channel.unit === threshold.unit).map(([key]) => key) : [idKey(threshold.channelId)];
    if (!targets.length) return undefined;
    for (const target of targets) {
      const channel = channels.get(target), key = target + ':' + threshold.family + ':' + threshold.name;
      if (channel === undefined || (level && threshold.unit !== channel.unit) || resolved.has(key)) return undefined;
      resolved.set(key, threshold);
    }
  }
  for (const [key, threshold] of resolved) {
    if (!['low','high'].includes(threshold.name)) continue;
    const prefix = key.slice(0,key.lastIndexOf(':') + 1), low = resolved.get(prefix + 'low'), high = resolved.get(prefix + 'high');
    if (low === undefined || high === undefined || low.value >= high.value) return undefined;
  }
  return resolved;
}
function measurementRequestSemantics(value: Profile): boolean {
  const ids = (value.channels ?? []) as Profile[], requested = (value.families ?? []) as string[], cursors = (value.cursors ?? []) as Profile[];
  if (!ids.length || !uniqueIds(ids) || !requested.length || new Set(requested).size !== requested.length) return false;
  if (!sourceSemantics(value.source, value.configuration) || !ids.some(v => idKey(v) === idKey(value.source.channelId))) return false;
  if (value.source.capture.revision.case === 'native' && value.source.capture.revision.value.value !== value.sourceRevision.value) return false;
  const available = new Map<string, Profile>(((value.configuration.channels ?? []) as Profile[]).map(v => [idKey(v.channelId),v])), channels = new Map<string, Profile>();
  for (const id of ids) { const key = idKey(id), channel = available.get(key); if (channel === undefined) return false; channels.set(key,channel); }
  const families = new Set(requested);
  if (thresholdBindings(value.referenceLevels ?? [], channels, families, false,true,false) === undefined || thresholdBindings(value.thresholds ?? [], channels, families, false,false,true) === undefined) return false;
  if (families.has('cursorDelta') ? cursors.length !== 2 : cursors.length !== 0) return false;
  for (const cursor of cursors) if (!channels.has(idKey(cursor.channelId)) || compareScopeTime(cursor.time,value.window.start) < 0 || compareScopeTime(cursor.time,value.window.end) >= 0) return false;
  return cursors.length !== 2 || channels.get(idKey(cursors[0]!.channelId))!.unit === channels.get(idKey(cursors[1]!.channelId))!.unit;
}
function measurementResultSemantics(value: Profile): boolean {
  if (!sourceSemantics(value.source,value.configuration) || value.runCount > value.finiteCount || value.requestedDuration.ticks <= 0n || value.coveredDuration.ticks < 0n || value.timingUncertainty.ticks < 0n || compareScopeTime(value.coveredDuration,value.requestedDuration) > 0) return false;
  if (value.finiteCount === 0n ? value.runCount !== 0n || value.coveredDuration.ticks !== 0n : value.runCount === 0n) return false;
  const channels = new Map<string, Profile>(((value.configuration.channels ?? []) as Profile[]).map(v => [idKey(v.channelId),v])), pairs = new Set<string>(), rows = (value.families ?? []) as Profile[], families = new Set<string>(rows.map(v => v.family));
  for (const family of rows) {
    const channel = channels.get(idKey(family.channelId)), pair = idKey(family.channelId) + ':' + family.family;
    if (channel === undefined || pairs.has(pair)) return false;
    pairs.add(pair);
    for (const item of (family.values ?? []) as Profile[]) if (fixedUnit(item.name) === undefined && item.unit !== channel.unit) return false;
  }
  const resolved = thresholdBindings(value.resolvedThresholds ?? [],channels,families,true,true,true);
  if (resolved === undefined || [...resolved.keys()].some(key => !pairs.has(key.slice(0,key.lastIndexOf(':'))))) return false;
  for (const family of rows.filter(v => v.status === 'ok' && pulseFamily(v.family))) {
    const prefix = idKey(family.channelId) + ':' + family.family + ':';
    if (['low','high','fraction10','fraction50','fraction90'].some(name => !resolved.has(prefix + name))) return false;
  }
  const cursorFamilies = rows.filter(v => v.family === 'cursorDelta' && v.status === 'ok');
  if ((value.cursor !== undefined) !== (cursorFamilies.length !== 0)) return false;
  if (value.cursor === undefined) return true;
  const left = channels.get(idKey(value.cursor.a.channelId)), right = channels.get(idKey(value.cursor.b.channelId));
  if (left === undefined || right === undefined || left.unit !== right.unit) return false;
  const seconds = Number(value.cursor.deltaTime.ticks) * Number(value.cursor.deltaTime.rate.denominator) / Number(value.cursor.deltaTime.rate.numerator);
  for (const family of cursorFamilies) {
    if (channels.get(idKey(family.channelId))!.unit !== left.unit) return false;
    for (const item of family.values as Profile[]) {
      const expected = item.name === 'deltaTime' ? seconds : value.cursor.deltaValue;
      if (Math.abs(item.result.value - expected) > 1e-12 + 1e-9 * Math.abs(expected)) return false;
    }
  }
  return true;
}
